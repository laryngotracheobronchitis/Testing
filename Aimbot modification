repeat
    task.wait()
until game:IsLoaded()

local UserInputService = game:GetService('UserInputService')
local RunService = game:GetService('RunService')

local u1 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*()_+[]{}|;:,.<>?/`~'
local function v4(p2)
    local v3 = ''
    for _ = 1, p2 do
        v3 = v3 .. string.sub(u1, math.random(1, #u1), math.random(1, #u1))
    end
    return v3
end

local screenGui = Instance.new('ScreenGui')
screenGui.Parent = game.CoreGui
screenGui.IgnoreGuiInset = true 
local frame = Instance.new('Frame')
frame.Size = UDim2.new(0, 140, 0, 140)
frame.Position = UDim2.new(0.5, 0, 0.5, 0) 
frame.AnchorPoint = Vector2.new(0.5, 0.5)
frame.BackgroundTransparency = 1
frame.Visible = false
frame.Parent = screenGui

local uIStroke = Instance.new('UIStroke')
uIStroke.Thickness = 5
uIStroke.Color = Color3.fromRGB(0, 0, 0)
uIStroke.Parent = frame

local function v11(p5, p6, p7, p8, p9, p10)
    local frame2 = Instance.new('Frame')
    frame2.Name = p5
    frame2.Size = p8
    frame2.Position = p7
    frame2.BackgroundColor3 = Color3.fromRGB(54, 57, 62)
    frame2.BackgroundTransparency = p10
    frame2.Visible = p9
    frame2.Parent = p6
    return frame2
end

local function v18(p12, p13, p14, p15, p16, p17)
    local textLabel = Instance.new('TextLabel')
    textLabel.Name = p12
    textLabel.Size = p15
    textLabel.Position = p14
    textLabel.BackgroundTransparency = 1
    textLabel.Text = p16
    textLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    textLabel.TextScaled = true
    textLabel.Font = Enum.Font.PatrickHand
    textLabel.Parent = p13
    textLabel.Rotation = p17
    return textLabel
end

local function v26(p19, p20, p21, p22, p23, p24, p25)
    local textButton = Instance.new('TextButton')
    textButton.Name = p19
    textButton.Size = p22
    textButton.Position = p21
    textButton.Text = p24
    textButton.BackgroundColor3 = Color3.fromRGB(54, 57, 62)
    textButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    textButton.BackgroundTransparency = p25
    textButton.Parent = p20
    textButton.Visible = p23
    return textButton
end

local function v30(p27, p28, p29)
    local uICorner = Instance.new('UICorner')
    uICorner.Name = p27
    uICorner.Parent = p28
    uICorner.CornerRadius = p29
    return uICorner
end

local v31 = v11('mainFrame', screenGui, UDim2.new(0, 202, 0, -10), UDim2.new(0, 250, 0, 43), true, 0.1)
v31.BorderSizePixel = 0

local dragDetector = Instance.new("UIDragDetector")
dragDetector.Parent = v31
v31.Active = true
v31.Draggable = true

local u32 = v11('mainFrame1', v31, UDim2.new(0, 1.5, 0, 53), UDim2.new(0, 247, 0, 190), false, 0.1)
local u33 = v11('AimTargetFrame', u32, UDim2.new(0, 10, 0, 115), UDim2.new(0, 195, 0, 60), false, 0.05)
local u34 = v11('settingsFrame', v31, UDim2.new(0, 317, 0, 53), UDim2.new(0, 247, 0, 230), false, 0.05)

local u35 = v11('sliderFrame', u34, UDim2.new(0, 10, 0, 105), UDim2.new(0, 227, 0, 20), true, 0)
u35.Active = false
u35.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
local u36 = v11('slider', u35, UDim2.new(0.14, -5, 0, -5), UDim2.new(0, 22, 0, 35), true, 0)
u36.Active = false
local u37 = v11('sliderFill', u35, UDim2.new(0.14, 0, 0, 0), UDim2.new(0.14, 0, 1, 0), true, 0)
u37.BackgroundColor3 = Color3.fromRGB(0, 255, 0)

local smoothFrame = v11('sliderFrameSmooth', u34, UDim2.new(0, 10, 0, 160), UDim2.new(0, 227, 0, 20), true, 0)
smoothFrame.Active = false
smoothFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
local smoothKnob = v11('sliderSmooth', smoothFrame, UDim2.new(0.15, -5, 0, -5), UDim2.new(0, 22, 0, 35), true, 0)
smoothKnob.Active = false
local smoothFill = v11('sliderFillSmooth', smoothFrame, UDim2.new(0, 0, 0, 0), UDim2.new(0.15, 0, 1, 0), true, 0)
smoothFill.BackgroundColor3 = Color3.fromRGB(0, 255, 255)

v18('Text', v31, UDim2.new(0, 5, 0, 10), UDim2.new(0, 120, 0, 20), 'Equinox Hub Aimbot', 0).Font = Enum.Font.ArimoBold
local v38 = v26('Button', v31, UDim2.new(0, 204, 0, 0), UDim2.new(0, 43, 0, 43), true, '', 1)
local v39 = v26('aimActiveButton', u32, UDim2.new(0, 10, 0, 20), UDim2.new(0, 195, 0, 38), true, 'Aim Active', 0)
local u40 = v26('aimTargetButton', u32, UDim2.new(0, 10, 0, 80), UDim2.new(0, 195, 0, 38), true, 'HEAD', 0)
local u41 = v26('HEAd', u33, UDim2.new(0, 10, 0, 10), UDim2.new(0, 190, 0, 20), false, 'HEAD', 0)
local u42 = v26('CHEST', u33, UDim2.new(0, 10, 0, 40), UDim2.new(0, 190, 0, 20), false, 'CHEST', 0)
local u43 = v26('CHEST', u33, UDim2.new(0, 10, 0, 10), UDim2.new(0, 190, 0, 20), true, 'CHEST', 0)
local u44 = v26('PelvisS', u33, UDim2.new(0, 10, 0, 40), UDim2.new(0, 190, 0, 20), true, 'PELVIS', 0)
local u45 = v11('aimActiveRedGreen', u32, UDim2.new(0, 210, 0, 24), UDim2.new(0, 30, 0, 30), true, 0)
u45.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
local u46 = v26('nextButton', u32, UDim2.new(0, 257, 0, 65), UDim2.new(0, 50, 0, 50), false, '>', 0)
local u47 = v18('Text1', v38, UDim2.new(0, -9.5, 0, 10), UDim2.new(0, 60, 0, 20), '^', 180)
u47.Font = Enum.Font.ArimoBold
u47.TextSize = 23
local v48 = v18('Text2', u32, UDim2.new(0, 36, 0, 60), UDim2.new(0, 140, 0, 20), 'Where should I aim?', 0)
v48.Font = Enum.Font.ArimoBold
v48.TextSize = 23
v18('Text3', u34, UDim2.new(0, 10, 0, 37), UDim2.new(0, 140, 0, 30), 'Circle color, RGB code example (255,255,255)', 0)
v48.Font = Enum.Font.ArimoBold
v48.TextSize = 23
local v49 = v18('Text4', u34, UDim2.new(0, 90, 0, 10), UDim2.new(0, 70, 0, 20), 'SETT\196\176NGS', 0)
v49.Font = Enum.Font.ArimoBold
v49.TextSize = 23
local v50 = v18('Text5', u34, UDim2.new(0, 10, 0, 70), UDim2.new(0, 80, 0, 20), 'Aimbot Fov: ', 0)
v50.Font = Enum.Font.ArimoBold
v50.TextSize = 23
v50.TextScaled = false
local u51 = v18('Text6', u34, UDim2.new(0, 82, 0, 70), UDim2.new(0, 60, 0, 20), '70', 0)
u51.Font = Enum.Font.ArimoBold
u51.TextSize = 23
u51.TextScaled = false

local sText = v18('TextSmooth', u34, UDim2.new(0, 10, 0, 125), UDim2.new(0, 100, 0, 20), 'Smoothness: ', 0)
sText.Font = Enum.Font.ArimoBold
sText.TextSize = 14
sText.TextScaled = false
local sValText = v18('TextSmoothVal', u34, UDim2.new(0, 100, 0, 125), UDim2.new(0, 60, 0, 20), '15', 0)
sValText.Font = Enum.Font.ArimoBold
sValText.TextSize = 14
sValText.TextScaled = false

local u59 = (function(p52, p53, p54, p55, p56, p57, p58)
    local textBox = Instance.new('TextBox')
    textBox.Name = p52
    textBox.Size = p55
    textBox.Position = p54
    textBox.Text = p57
    textBox.BackgroundColor3 = Color3.fromRGB(43, 43, 43)
    textBox.TextColor3 = Color3.fromRGB(255, 255, 255)
    textBox.BackgroundTransparency = p58
    textBox.Parent = p53
    textBox.Visible = p56
    return textBox
end)('TextBox', u34, UDim2.new(0, 185, 0, 30), UDim2.new(0, 40, 0, 40), true, '0, 0, 0', 0)
u59.Font = Enum.Font.ArialBold
u59.TextSize = 10
u59.BorderSizePixel = 0

local povBtn = v26('povBtn', u34, UDim2.new(0, 10, 0, 185), UDim2.new(0, 110, 0, 30), true, 'POV: OFF', 0)
povBtn.Font = Enum.Font.ArimoBold
povBtn.TextScaled = false
povBtn.TextSize = 14
povBtn.BackgroundColor3 = Color3.fromRGB(255, 0, 0)

local teamBtn = v26('teamBtn', u34, UDim2.new(0, 127, 0, 185), UDim2.new(0, 110, 0, 30), true, 'TeamCheck: ON', 0)
teamBtn.Font = Enum.Font.ArimoBold
teamBtn.TextScaled = false
teamBtn.TextSize = 14
teamBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 0)

v30('uic', v31, UDim.new(0, 13))
v30('uic1', u32, UDim.new(0, 12))
v30('uic2', v39, UDim.new(0, 12))
v30('uic3', u45, UDim.new(1, 1))
v30('uic4', u40, UDim.new(0, 12))
v30('uic5', u41, UDim.new(0, 12))
v30('uic6', u42, UDim.new(0, 12))
v30('uic7', u43, UDim.new(0, 12))
v30('uic8', u44, UDim.new(0, 12))
v30('uic9', u33, UDim.new(0, 12))
v30('uic10', frame, UDim.new(1, 0))
v30('uic11', u46, UDim.new(0, 13))
v30('uic12', u34, UDim.new(0, 12))
v30('uic13', u59, UDim.new(0, 12))
v30('uicSmooth1', smoothFrame, UDim.new(0, 0))
v30('uicSmooth2', smoothKnob, UDim.new(0, 0))
v30('uicPov', povBtn, UDim.new(0, 8))
v30('uicTeam', teamBtn, UDim.new(0, 8))

local u60 = {
    {
        Head = true,
        Chest = false,
        Pelvis = false
    }
}
local u61 = false
local u62 = false
local u64 = false
local Smoothness = 0.15
local povVisible = false
local teamCheckEnabled = true
local lockedTarget = nil -- Variabel untuk menyimpan target yang terkunci

v38.MouseButton1Click:Connect(function()
    if u62 ~= false then
        u62 = false
        u47.Rotation = 180
        u32.Visible = false
        u46.Visible = false
        u46.Text = '>'
        u61 = false
        u34.Visible = false
    else
        u62 = true
        u47.Rotation = 0
        u32.Visible = true
        u46.Visible = true
        u46.Text = '>'
        u61 = false
    end
end)

u46.MouseButton1Click:Connect(function()
    if u61 ~= false then
        u46.Text = '>'
        u34.Visible = false
        u61 = false
    else
        u46.Text = '<'
        u34.Visible = true
        u61 = true
    end
end)

local u63 = false
u40.MouseButton1Click:Connect(function()
    u63 = not u63
    u33.Visible = u63
end)

u42.MouseButton1Click:Connect(function()
    u44.Visible = false
    u41.Visible = true
    u43.Visible = false
    u42.Visible = true
    u60[1].Head = false
    u60[1].Chest = true
    u60[1].Pelvis = false
    u40.Text = 'CHEST'
    u63 = false
    u33.Visible = false
    lockedTarget = nil -- Reset target jika mode aim diganti
end)

u43.MouseButton1Click:Connect(function()
    u44.Visible = true
    u41.Visible = true
    u43.Visible = false
    u42.Visible = false
    u60[1].Head = false
    u60[1].Chest = true
    u60[1].Pelvis = false
    u40.Text = 'CHEST'
    u63 = false
    u33.Visible = false
    lockedTarget = nil
end)

u44.MouseButton1Click:Connect(function()
    u44.Visible = false
    u41.Visible = true
    u43.Visible = false
    u42.Visible = true
    u60[1].Head = false
    u60[1].Chest = false
    u60[1].Pelvis = true
    u40.Text = 'PELVIS'
    u63 = false
    u33.Visible = false
    lockedTarget = nil
end)

u41.MouseButton1Click:Connect(function()
    u43.Visible = true
    u44.Visible = true
    u41.Visible = false
    u42.Visible = false
    u60[1].Head = true
    u60[1].Chest = false
    u60[1].Pelvis = false
    u40.Text = 'HEAD'
    u63 = false
    u33.Visible = false
    lockedTarget = nil
end)

povBtn.MouseButton1Click:Connect(function()
    povVisible = not povVisible
    if u64 then
        frame.Visible = povVisible
    end
    if povVisible then
        povBtn.Text = 'POV: ON'
        povBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
    else
        povBtn.Text = 'POV: OFF'
        povBtn.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    end
end)

teamBtn.MouseButton1Click:Connect(function()
    teamCheckEnabled = not teamCheckEnabled
    if teamCheckEnabled then
        teamBtn.Text = 'TeamCheck: ON'
        teamBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
    else
        teamBtn.Text = 'TeamCheck: OFF'
        teamBtn.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    end
end)

local localPlayer = game.Players.LocalPlayer
local currentCamera = game.Workspace.CurrentCamera

-- Helper untuk mendapatkan part yang akan di-aim sesuai pilihan (Head/Chest/Pelvis)
local function getAimPart(character)
    if u60[1].Head and character:FindFirstChild('Head') then
        return character.Head
    elseif u60[1].Chest and character:FindFirstChild('HumanoidRootPart') then
        return character.HumanoidRootPart
    elseif u60[1].Pelvis and character:FindFirstChild('LowerTorso') then
        return character.LowerTorso
    elseif character:FindFirstChild('Head') then
        return character.Head -- Fallback jika pilihan tidak ada
    end
    return nil
end

local function u74()
    local centerScreen = Vector2.new(currentCamera.ViewportSize.X / 2, currentCamera.ViewportSize.Y / 2)
    local radius = frame.Size.X.Offset / 2
    local ignoreChar = localPlayer.Character
    
    -- 1. Cek apakah target yang sudah terkunci masih valid
    if lockedTarget and lockedTarget.Character and lockedTarget.Character:FindFirstChild('Humanoid') and lockedTarget.Character.Humanoid.Health > 0 then
        local part = getAimPart(lockedTarget.Character)
        if part then
            local screenPos, onScreen = currentCamera:WorldToScreenPoint(part.Position)
            local magnitude = (Vector2.new(screenPos.X, screenPos.Y) - centerScreen).Magnitude
            local isTeammate = (teamCheckEnabled and localPlayer.Team and lockedTarget.Team == localPlayer.Team)
            
            -- Jika masih di layar, masih di dalam FOV, bukan teman, dan tidak kena tembok
            if not isTeammate and onScreen and magnitude <= radius then
                local ray = Ray.new(currentCamera.CFrame.Position, (part.Position - currentCamera.CFrame.Position).Unit * 3000)
                local hit = game.Workspace:FindPartOnRay(ray, ignoreChar, false, true)
                if hit and hit:IsDescendantOf(lockedTarget.Character) then
                    return lockedTarget -- Kunci tetap pada target ini!
                end
            end
        end
    end
    
    -- 2. Jika target lama tidak valid, reset dan cari target baru
    lockedTarget = nil
    local closestMagnitude = math.huge
    local newTarget = nil
    
    for _, player in ipairs(game.Players:GetPlayers()) do
        if player ~= localPlayer and player.Character and player.Character:FindFirstChild('Humanoid') and player.Character.Humanoid.Health > 0 then
            local part = getAimPart(player.Character)
            local isTeammate = (teamCheckEnabled and localPlayer.Team and player.Team == localPlayer.Team)
            
            if part and not isTeammate then
                local screenPos, onScreen = currentCamera:WorldToScreenPoint(part.Position)
                local magnitude = (Vector2.new(screenPos.X, screenPos.Y) - centerScreen).Magnitude
                
                if onScreen and magnitude < closestMagnitude and magnitude <= radius then
                    local ray = Ray.new(currentCamera.CFrame.Position, (part.Position - currentCamera.CFrame.Position).Unit * 3000)
                    local hit = game.Workspace:FindPartOnRay(ray, ignoreChar, false, true)
                    if hit and hit:IsDescendantOf(player.Character) then
                        newTarget = player
                        closestMagnitude = magnitude
                    end
                end
            end
        end
    end
    
    lockedTarget = newTarget
    return newTarget
end

local function v76()
    if u64 then
        local v75 = u74()
        if v75 and v75.Character then
            local aimPart = getAimPart(v75.Character)
            if aimPart then
                local targetCFrame = CFrame.new(currentCamera.CFrame.Position, aimPart.Position)
                if Smoothness >= 0.99 then
                    currentCamera.CFrame = targetCFrame
                else
                    currentCamera.CFrame = currentCamera.CFrame:Lerp(targetCFrame, Smoothness)
                end
            end
        end
    end
end

RunService.RenderStepped:Connect(v76)

local u77 = false
v39.MouseButton1Click:Connect(function()
    if u77 ~= false then
        u77 = false
        frame.Visible = false
        u64 = false
        u45.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
        lockedTarget = nil -- Reset lock saat aimbot dimatikan
    else
        u77 = true
        frame.Visible = povVisible
        u64 = true
        u45.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
    end
end)

local function u85(p78)
    local v79, v80, v81 = p78:match('(%d+),%s*(%d+),%s*(%d+)')
    local v82 = tonumber(v79)
    local v83 = tonumber(v80)
    local v84 = tonumber(v81)
    if v82 and (v83 and (v84 and (0 <= v82 and (v82 <= 255 and (0 <= v83 and (v83 <= 255 and (0 <= v84 and v84 <= 255))))))) then
        return true, Color3.fromRGB(v82, v83, v84)
    else
        return false, Color3.fromRGB(0, 0, 0)
    end
end

u59.FocusLost:Connect(function(p86)
    if p86 then
        local v87, v88 = u85(u59.Text)
        if v87 then
            uIStroke.Color = v88
        else
            uIStroke.Color = Color3.fromRGB(0, 0, 0)
        end
    end
end)

local function u95(p92)
    local v93 = math.clamp((p92.Position.X - u35.AbsolutePosition.X) / u35.AbsoluteSize.X, 0, 1)
    local v94 = math.floor(v93 * 500)
    u36.Position = UDim2.new(v93, -5, 0, -5)
    u37.Size = UDim2.new(v93, 0, 1, 0)
    u51.Text = tostring(v94)
    frame.Size = UDim2.new(0, v94 * 2, 0, v94 * 2)
end

local u96 = false
u36.InputBegan:Connect(function(p97)
    if p97.UserInputType == Enum.UserInputType.MouseButton1 or p97.UserInputType == Enum.UserInputType.Touch then
        u96 = true
        u95(p97)
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if u96 and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        u95(input)
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        u96 = false
    end
end)

local function updateSmooth(input)
    local rel = math.clamp((input.Position.X - smoothFrame.AbsolutePosition.X) / smoothFrame.AbsoluteSize.X, 0, 1)
    local val = math.floor(rel * 100)
    Smoothness = val / 100
    smoothKnob.Position = UDim2.new(rel, -5, 0, -5)
    smoothFill.Size = UDim2.new(rel, 0, 1, 0)
    sValText.Text = tostring(val)
end

local smoothDragging = false
smoothKnob.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        smoothDragging = true
        updateSmooth(input)
    end
end)
smoothFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        smoothDragging = true
        updateSmooth(input)
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if smoothDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        updateSmooth(input)
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        smoothDragging = false
    end
end)

u95({
    Position = Vector3.new(u35.AbsolutePosition.X + u35.AbsoluteSize.X * 0.14, 0, 0)
})

local v100 = { screenGui }
while task.wait(2) do
    local v101, v102, v103 = pairs(v100)
    while true do
        local v104
        v103, v104 = v101(v102, v103)
        if v103 == nil then
            break
        end
        v104.Name = v4(15)
    end
end
