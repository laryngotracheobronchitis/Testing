repeat task.wait() until game:IsLoaded()

P = game:GetService("Players")
R = game:GetService("RunService")
RS = game:GetService("ReplicatedStorage")
L = game:GetService("Lighting")
UI = game:GetService("UserInputService")
TS = game:GetService("TweenService")
DB = game:GetService("Debris")
Stats = game:GetService("Stats")

pl = P.LocalPlayer
pg = pl:WaitForChild("PlayerGui")
mb = UI.TouchEnabled and not UI.KeyboardEnabled
pcMode = false

for _, v in ipairs(pg:GetChildren()) do
    if v.Name == "ENX" then
        v:Destroy()
    end
end

pcall(function()
    local mt = getrawmetatable(game)
    if mt then
        local old = mt.__namecall
        setreadonly(mt, false)
        mt.__namecall = newcclosure(function(self, ...)
            if getnamecallmethod():lower() == "kick" and self == pl then
                return
            end
            return old(self, ...)
        end)
        setreadonly(mt, true)
    end
end)

S = {
    AP = false, AS = false, BE = false, PE = false, RE = false, DE = false,
    FP = false, BT = false, AD = false, FB = false, VT = false, AE = false,
    SI = false, SK = false, ID = false, TD = false, DD = false, AF = false,
    BF = false, TN = false, AE2 = false, SR = false, PD = false, HS = false,
    IW = false, TB = false, SG = false, LAP = false, LAS = false, KS = false,
    NR = false, NR2 = false, SC = false
}

orbitR = 20
orbitH = 5
savedWS = nil
savedJP = nil
revertedRemotes = {}
hasData = false
serverFireCounter = 0
peakSpeed = 0
Parries = 0
Speed_Divisor_Multiplier = 1.1
ParryThreshold = 1
Closest_Entity = nil
Infinity = false
Phantom = false
siAngle = 0
ms = false
lobbySpamDB = 0
trainingParried = false
Balls = nil
Runtime = nil
Alive = nil
TrainingBalls = nil
VTL1 = nil
VTL2 = nil
VTL3 = nil
VTUI = nil
ST = nil
SD = nil
hitSound = nil
HL = {}
BHL = nil
TLb = nil
AEGUI = {}
abilityDB = {}
pAbCache = {}
aeLabels = {}
musicSound = nil
currentMusicIdx = 0
songBtns = {}
nowPlayingLabel = nil
NOTIF_TL = nil
NOTIF = nil
op = false
C = nil
allUI = {}
VTUI_REF = {}
TB2 = {}
TP = {}
MF = nil
SG2 = nil
OB = nil
MS = nil
MD = nil
MkTab = nil
MkSec = nil
MkTog = nil
MkSlider = nil
CP = nil
EP = nil
XP = nil
MU = nil
MP = nil
SB = nil
ShowNotif = nil
doFire = nil
isStaff = nil
KSS_selected = "92076037937225"
KSS_lastKill = 0
KSS_conns = {}
KSS_curSound = nil
swordModel = ""
swordAnimations = ""
swordFX = ""
skinChangerEnabled = false
noRenderConn = nil
noRender2Conn = nil
siLV = nil
siAtt = nil
AE_STATE = {active = false, players = {}}
Network = Stats.Network

Sys = {
    __parried = false,
    __training_parried = false,
    __antidot_parried = false,
    __grab_animation = nil,
    __play_animation = false,
    __curve_mode = 1,
    __infinity_active = false,
    __deathslash_active = false,
    __timehole_active = false,
    __tornado_time = tick(),
    __spam_target = nil,
    __spam_target_time = 0,
    __last_closest = tick(),
    __auto_spam_enabled = false,
    __first_parry_done = false
}

function applyStats(ch)
    local hum = ch and ch:FindFirstChildOfClass("Humanoid")
    if not hum then return end
    if savedWS then hum.WalkSpeed = savedWS end
    if savedJP then hum.JumpPower = savedJP end
end

pl.CharacterAdded:Connect(function(ch)
    task.wait(0.5)
    applyStats(ch)
    peakSpeed = 0
end)

local THEMES = {
    Ocean = {BG = Color3.fromRGB(4,12,24), P = Color3.fromRGB(6,18,36), C = Color3.fromRGB(8,24,48), A = Color3.fromRGB(0,180,220), A2 = Color3.fromRGB(0,230,255), TX = Color3.fromRGB(220,245,255), SB = Color3.fromRGB(100,160,190), ON = Color3.fromRGB(0,220,140), OF = Color3.fromRGB(20,40,60)},
    Emerald = {BG = Color3.fromRGB(4,14,8), P = Color3.fromRGB(6,20,12), C = Color3.fromRGB(8,28,16), A = Color3.fromRGB(0,200,80), A2 = Color3.fromRGB(0,255,120), TX = Color3.fromRGB(220,255,230), SB = Color3.fromRGB(80,160,100), ON = Color3.fromRGB(0,230,100), OF = Color3.fromRGB(15,40,20)},
    Royalty = {BG = Color3.fromRGB(10,6,20), P = Color3.fromRGB(16,10,30), C = Color3.fromRGB(22,14,42), A = Color3.fromRGB(180,100,255), A2 = Color3.fromRGB(220,160,255), TX = Color3.fromRGB(240,230,255), SB = Color3.fromRGB(140,110,180), ON = Color3.fromRGB(0,220,140), OF = Color3.fromRGB(35,20,55)},
    Purple = {BG = Color3.fromRGB(8,6,18), P = Color3.fromRGB(14,10,28), C = Color3.fromRGB(20,15,38), A = Color3.fromRGB(120,60,220), A2 = Color3.fromRGB(160,100,255), TX = Color3.fromRGB(240,235,255), SB = Color3.fromRGB(120,100,160), ON = Color3.fromRGB(0,220,140), OF = Color3.fromRGB(30,20,50)},
    Crimson = {BG = Color3.fromRGB(16,4,8), P = Color3.fromRGB(24,6,12), C = Color3.fromRGB(32,8,16), A = Color3.fromRGB(220,40,60), A2 = Color3.fromRGB(255,80,100), TX = Color3.fromRGB(255,230,235), SB = Color3.fromRGB(180,100,110), ON = Color3.fromRGB(0,220,140), OF = Color3.fromRGB(50,15,20)},
    Midnight = {BG = Color3.fromRGB(6,6,10), P = Color3.fromRGB(10,10,16), C = Color3.fromRGB(16,16,24), A = Color3.fromRGB(100,120,255), A2 = Color3.fromRGB(140,160,255), TX = Color3.fromRGB(230,235,255), SB = Color3.fromRGB(110,115,150), ON = Color3.fromRGB(0,220,140), OF = Color3.fromRGB(25,25,40)}
}

C = THEMES.Ocean

local function N(cl, par, pr)
    local o = Instance.new(cl)
    for k, v in pairs(pr or {}) do
        pcall(function() o[k] = v end)
    end
    o.Parent = par
    return o
end

local function TW(o, t, p)
    pcall(function()
        TS:Create(o, TweenInfo.new(t, Enum.EasingStyle.Quart), p):Play()
    end)
end

local function RI(obj, prop, key)
    table.insert(allUI, {obj = obj, prop = prop, key = key})
end

local W, H = mb and 310 or 430, mb and 520 or 590

local function Drag(h, tg)
    local dr, ds, sp, di = false
    h.InputBegan:Connect(function(n)
        if n.UserInputType == Enum.UserInputType.MouseButton1 or n.UserInputType == Enum.UserInputType.Touch then
            dr, ds, sp = true, n.Position, tg.Position
            n.Changed:Connect(function()
                if n.UserInputState == Enum.UserInputState.End then dr = false end
            end)
        end
    end)
    h.InputChanged:Connect(function(n)
        if n.UserInputType == Enum.UserInputType.MouseMovement or n.UserInputType == Enum.UserInputType.Touch then
            di = n
        end
    end)
    UI.InputChanged:Connect(function(n)
        if n == di and dr then
            local d = n.Position - ds
            tg.Position = UDim2.new(sp.X.Scale, sp.X.Offset + d.X, sp.Y.Scale, sp.Y.Offset + d.Y)
        end
    end)
end

SG2 = N("ScreenGui", pg, {Name = "ENX", ResetOnSpawn = false, IgnoreGuiInset = true, DisplayOrder = 10})

local Mn = N("Frame", SG2, {Size = UDim2.fromOffset(mb and 200 or 260, 40), Position = UDim2.new(0.5, -130, 0, 8), BackgroundColor3 = C.P, BorderSizePixel = 0, ZIndex = 20})
N("UICorner", Mn, {CornerRadius = UDim.new(0, 20)})
MS = N("UIStroke", Mn, {Thickness = 1.5, Color = C.A, Transparency = 0.2})
RI(MS, "Color", "A")
MD = N("Frame", Mn, {Size = UDim2.fromOffset(8, 8), Position = UDim2.new(0, 11, 0.5, -4), BackgroundColor3 = C.ON, ZIndex = 21})
N("UICorner", MD, {CornerRadius = UDim.new(1, 0)})
N("TextLabel", Mn, {Size = UDim2.new(1, -65, 1, 0), Position = UDim2.new(0, 24, 0, 0), BackgroundTransparency = 1, Text = "EclipseNexus", Font = Enum.Font.GothamBold, TextSize = 12, TextColor3 = C.TX, TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 21})
OB = N("TextButton", Mn, {Size = UDim2.fromOffset(52, 26), Position = UDim2.new(1, -58, 0.5, -13), BackgroundColor3 = C.A, Text = "OPEN", Font = Enum.Font.GothamBold, TextSize = 10, TextColor3 = Color3.new(1,1,1), AutoButtonColor = false, ZIndex = 22})
RI(OB, "BackgroundColor3", "A")
N("UICorner", OB, {CornerRadius = UDim.new(0, 8)})
Drag(Mn, Mn)

MF = N("Frame", SG2, {Size = UDim2.fromOffset(W, H), Position = UDim2.new(0.5, -W/2, 0.5, -H/2), BackgroundColor3 = C.BG, BorderSizePixel = 0, Visible = false, ZIndex = 1})
N("UICorner", MF, {CornerRadius = UDim.new(0, 18)})
local MFS = N("UIStroke", MF, {Thickness = 1.5, Color = C.A, Transparency = 0.3, ApplyStrokeMode = Enum.ApplyStrokeMode.Border})
RI(MFS, "Color", "A")
RI(MF, "BackgroundColor3", "BG")
Drag(MF, MF)

OB.MouseButton1Click:Connect(function()
    op = not op
    if op then
        MF.Visible = true
        MF.Size = UDim2.fromOffset(0, 0)
        TW(MF, 0.35, {Size = UDim2.fromOffset(W, H)})
        OB.Text = "CLOSE"
        TW(OB, 0.2, {BackgroundColor3 = Color3.fromRGB(200,40,40)})
    else
        TW(MF, 0.25, {Size = UDim2.fromOffset(0, 0)})
        task.delay(0.28, function() MF.Visible = false end)
        OB.Text = "OPEN"
        TW(OB, 0.2, {BackgroundColor3 = C.A})
    end
end)

local HDR = N("Frame", MF, {Size = UDim2.new(1, 0, 0, 62), BackgroundColor3 = C.P, BorderSizePixel = 0, ZIndex = 2})
N("UICorner", HDR, {CornerRadius = UDim.new(0, 18)})
N("Frame", HDR, {Size = UDim2.new(1, 0, 0, 18), Position = UDim2.new(0, 0, 1, -18), BackgroundColor3 = C.P, BorderSizePixel = 0, ZIndex = 3})
RI(HDR, "BackgroundColor3", "P")

local LC = N("Frame", HDR, {Size = UDim2.fromOffset(44, 44), Position = UDim2.new(0, 10, 0.5, -22), BackgroundColor3 = Color3.fromRGB(2,2,8), ZIndex = 4})
N("UICorner", LC, {CornerRadius = UDim.new(1, 0)})
local LCS = N("UIStroke", LC, {Thickness = 2, Color = C.A})
RI(LCS, "Color", "A")
local LCShadow = N("Frame", LC, {Size = UDim2.fromOffset(40, 40), Position = UDim2.fromOffset(10, -4), BackgroundColor3 = C.P, ZIndex = 5})
N("UICorner", LCShadow, {CornerRadius = UDim.new(1, 0)})
RI(LCShadow, "BackgroundColor3", "P")
N("TextLabel", LC, {Size = UDim2.new(1, 0, 1, 0), BackgroundTransparency = 1, Text = "E", Font = Enum.Font.GothamBold, TextSize = 24, TextColor3 = C.A, ZIndex = 6})

local TL1 = N("TextLabel", HDR, {Size = UDim2.new(1, -110, 0, 22), Position = UDim2.new(0, 62, 0, 8), BackgroundTransparency = 1, Text = "ECLIPSENEXUS", Font = Enum.Font.GothamBold, TextSize = 15, TextColor3 = C.TX, TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 4})
RI(TL1, "TextColor3", "TX")
local TL2 = N("TextLabel", HDR, {Size = UDim2.new(1, -110, 0, 14), Position = UDim2.new(0, 62, 0, 30), BackgroundTransparency = 1, Text = "v2.0 BladeBall", Font = Enum.Font.Gotham, TextSize = 10, TextColor3 = C.A, TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 4})
RI(TL2, "TextColor3", "A")
local acbar = N("Frame", HDR, {Size = UDim2.new(1, -20, 0, 2), Position = UDim2.new(0, 10, 1, -3), BackgroundColor3 = C.A, BorderSizePixel = 0, ZIndex = 4})
N("UICorner", acbar, {CornerRadius = UDim.new(1, 0)})
RI(acbar, "BackgroundColor3", "A")

local THB = N("Frame", MF, {Size = UDim2.new(1, -16, 0, 28), Position = UDim2.new(0, 8, 0, 66), BackgroundColor3 = C.P, BorderSizePixel = 0, ZIndex = 2})
N("UICorner", THB, {CornerRadius = UDim.new(0, 8)})
RI(THB, "BackgroundColor3", "P")
N("UIListLayout", THB, {FillDirection = Enum.FillDirection.Horizontal, Padding = UDim.new(0, 5), HorizontalAlignment = Enum.HorizontalAlignment.Center, VerticalAlignment = Enum.VerticalAlignment.Center})

local tDots = {}
local tNames = {"Ocean", "Emerald", "Royalty", "Purple", "Crimson", "Midnight"}

local THEMES2 = {
    Ocean = {BG = Color3.fromRGB(4,12,24), P = Color3.fromRGB(6,18,36), C = Color3.fromRGB(8,24,48), A = Color3.fromRGB(0,180,220), A2 = Color3.fromRGB(0,230,255), TX = Color3.fromRGB(220,245,255), SB = Color3.fromRGB(100,160,190), ON = Color3.fromRGB(0,220,140), OF = Color3.fromRGB(20,40,60)},
    Emerald = {BG = Color3.fromRGB(4,14,8), P = Color3.fromRGB(6,20,12), C = Color3.fromRGB(8,28,16), A = Color3.fromRGB(0,200,80), A2 = Color3.fromRGB(0,255,120), TX = Color3.fromRGB(220,255,230), SB = Color3.fromRGB(80,160,100), ON = Color3.fromRGB(0,230,100), OF = Color3.fromRGB(15,40,20)},
    Royalty = {BG = Color3.fromRGB(10,6,20), P = Color3.fromRGB(16,10,30), C = Color3.fromRGB(22,14,42), A = Color3.fromRGB(180,100,255), A2 = Color3.fromRGB(220,160,255), TX = Color3.fromRGB(240,230,255), SB = Color3.fromRGB(140,110,180), ON = Color3.fromRGB(0,220,140), OF = Color3.fromRGB(35,20,55)},
    Purple = {BG = Color3.fromRGB(8,6,18), P = Color3.fromRGB(14,10,28), C = Color3.fromRGB(20,15,38), A = Color3.fromRGB(120,60,220), A2 = Color3.fromRGB(160,100,255), TX = Color3.fromRGB(240,235,255), SB = Color3.fromRGB(120,100,160), ON = Color3.fromRGB(0,220,140), OF = Color3.fromRGB(30,20,50)},
    Crimson = {BG = Color3.fromRGB(16,4,8), P = Color3.fromRGB(24,6,12), C = Color3.fromRGB(32,8,16), A = Color3.fromRGB(220,40,60), A2 = Color3.fromRGB(255,80,100), TX = Color3.fromRGB(255,230,235), SB = Color3.fromRGB(180,100,110), ON = Color3.fromRGB(0,220,140), OF = Color3.fromRGB(50,15,20)},
    Midnight = {BG = Color3.fromRGB(6,6,10), P = Color3.fromRGB(10,10,16), C = Color3.fromRGB(16,16,24), A = Color3.fromRGB(100,120,255), A2 = Color3.fromRGB(140,160,255), TX = Color3.fromRGB(230,235,255), SB = Color3.fromRGB(110,115,150), ON = Color3.fromRGB(0,220,140), OF = Color3.fromRGB(25,25,40)}
}

local function applyTheme(name)
    C = THEMES2[name]
    for _, u in ipairs(allUI) do
        pcall(function() u.obj[u.prop] = C[u.key] end)
    end
    for tn, dt in pairs(tDots) do
        local s = dt:FindFirstChildOfClass("UIStroke")
        if s then s:Destroy() end
        if tn == name then
            N("UIStroke", dt, {Thickness = 2, Color = Color3.new(1,1,1)})
        end
    end
    if not op then
        TW(OB, 0.1, {BackgroundColor3 = C.A})
    end
    pcall(function()
        if VTUI_REF.frame then VTUI_REF.frame.BackgroundColor3 = C.P end
        if VTUI_REF.stroke then VTUI_REF.stroke.Color = C.A end
        LCShadow.BackgroundColor3 = C.P
    end)
end

for _, tn in ipairs(tNames) do
    local dot = N("TextButton", THB, {Size = UDim2.fromOffset(16, 16), BackgroundColor3 = THEMES2[tn].A, Text = "", AutoButtonColor = false, ZIndex = 3})
    N("UICorner", dot, {CornerRadius = UDim.new(1, 0)})
    tDots[tn] = dot
    local tnc = tn
    dot.MouseButton1Click:Connect(function() applyTheme(tnc) end)
end

local TABBAR = N("Frame", MF, {Size = UDim2.new(1, -16, 0, 30), Position = UDim2.new(0, 8, 0, 100), BackgroundColor3 = C.P, BorderSizePixel = 0, ZIndex = 2})
N("UICorner", TABBAR, {CornerRadius = UDim.new(0, 10)})
RI(TABBAR, "BackgroundColor3", "P")
N("UIListLayout", TABBAR, {FillDirection = Enum.FillDirection.Horizontal, Padding = UDim.new(0, 2), HorizontalAlignment = Enum.HorizontalAlignment.Center, VerticalAlignment = Enum.VerticalAlignment.Center})

local PC = N("Frame", MF, {Size = UDim2.new(1, -16, 1, -172), Position = UDim2.new(0, 8, 0, 136), BackgroundTransparency = 1, ClipsDescendants = true, ZIndex = 2})

local SBar = N("Frame", MF, {Size = UDim2.new(1, -16, 0, 24), Position = UDim2.new(0, 8, 1, -30), BackgroundColor3 = C.P, BorderSizePixel = 0, ZIndex = 2})
N("UICorner", SBar, {CornerRadius = UDim.new(0, 8)})
local SBS = N("UIStroke", SBar, {Thickness = 1, Color = C.A, Transparency = 0.6})
RI(SBS, "Color", "A")
RI(SBar, "BackgroundColor3", "P")
SD = N("Frame", SBar, {Size = UDim2.fromOffset(7, 7), Position = UDim2.new(0, 9, 0.5, -3.5), BackgroundColor3 = C.ON, ZIndex = 3})
N("UICorner", SD, {CornerRadius = UDim.new(1, 0)})
ST = N("TextLabel", SBar, {Size = UDim2.new(1, -24, 1, 0), Position = UDim2.new(0, 23, 0, 0), BackgroundTransparency = 1, Text = "EclipseNexus Loading...", Font = Enum.Font.Gotham, TextSize = 10, TextColor3 = C.SB, TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 3})
RI(ST, "TextColor3", "SB")

local function Drag2(h, tg)
    local dr, ds, sp, di = false
    h.InputBegan:Connect(function(n)
        if n.UserInputType == Enum.UserInputType.MouseButton1 or n.UserInputType == Enum.UserInputType.Touch then
            dr, ds, sp = true, n.Position, tg.Position
            n.Changed:Connect(function()
                if n.UserInputState == Enum.UserInputState.End then dr = false end
            end)
        end
    end)
    h.InputChanged:Connect(function(n)
        if n.UserInputType == Enum.UserInputType.MouseMovement or n.UserInputType == Enum.UserInputType.Touch then
            di = n
        end
    end)
    UI.InputChanged:Connect(function(n)
        if n == di and dr then
            local d = n.Position - ds
            tg.Position = UDim2.new(sp.X.Scale, sp.X.Offset + d.X, sp.Y.Scale, sp.Y.Offset + d.Y)
        end
    end)
end

MkTab = function(nm, ic)
    local sz = mb and 34 or 44
    local B = N("TextButton", TABBAR, {Size = UDim2.fromOffset(sz, 24), BackgroundColor3 = C.C, Text = (ic or "") .. nm, Font = Enum.Font.GothamMedium, TextSize = 8, TextColor3 = C.SB, AutoButtonColor = false, ZIndex = 3})
    N("UICorner", B, {CornerRadius = UDim.new(0, 8)})
    RI(B, "BackgroundColor3", "C")
    local Pg = N("ScrollingFrame", PC, {Size = UDim2.new(1, 0, 1, 0), BackgroundTransparency = 1, ScrollBarThickness = 3, ScrollBarImageColor3 = C.A, Visible = false, CanvasSize = UDim2.new(0, 0, 0, 0), AutomaticCanvasSize = Enum.AutomaticSize.Y, BorderSizePixel = 0, ZIndex = 2})
    N("UIListLayout", Pg, {Padding = UDim.new(0, 5)})
    N("UIPadding", Pg, {PaddingTop = UDim.new(0, 4), PaddingBottom = UDim.new(0, 8), PaddingLeft = UDim.new(0, 2), PaddingRight = UDim.new(0, 6)})
    TB2[nm] = B
    TP[nm] = Pg
    B.MouseButton1Click:Connect(function()
        for n, p in pairs(TP) do
            p.Visible = false
            TB2[n].TextColor3 = C.SB
            TW(TB2[n], 0.15, {BackgroundColor3 = C.C})
        end
        Pg.Visible = true
        B.TextColor3 = C.TX
        TW(B, 0.15, {BackgroundColor3 = C.A})
    end)
    return Pg
end

MkSec = function(par, txt)
    local F = N("Frame", par, {Size = UDim2.new(1, 0, 0, 20), BackgroundTransparency = 1, ZIndex = 2})
    N("Frame", F, {Size = UDim2.new(1, 0, 0, 1), Position = UDim2.new(0, 0, 0.5, 0), BackgroundColor3 = C.OF, BorderSizePixel = 0, ZIndex = 2})
    local bg = N("Frame", F, {Size = UDim2.fromOffset(#txt * 7 + 14, 14), Position = UDim2.new(0, 6, 0.5, -7), BackgroundColor3 = C.BG, BorderSizePixel = 0, ZIndex = 3})
    N("TextLabel", bg, {Size = UDim2.new(1, 0, 1, 0), BackgroundTransparency = 1, Text = txt:upper(), Font = Enum.Font.GothamBold, TextSize = 9, TextColor3 = C.A, TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 4})
end

MkTog = function(par, lbl, key, desc)
    local ht = desc and (mb and 54 or 44) or (mb and 38 or 32)
    local Cd = N("Frame", par, {Size = UDim2.new(1, 0, 0, ht), BackgroundColor3 = C.C, BorderSizePixel = 0, ZIndex = 2})
    N("UICorner", Cd, {CornerRadius = UDim.new(0, 10)})
    N("UIStroke", Cd, {Thickness = 1, Color = C.OF})
    local lb = N("Frame", Cd, {Size = UDim2.new(0, 3, 0.6, 0), Position = UDim2.new(0, 0, 0.2, 0), BackgroundColor3 = C.OF, BorderSizePixel = 0, ZIndex = 3})
    N("UICorner", lb, {CornerRadius = UDim.new(1, 0)})
    N("TextLabel", Cd, {Size = UDim2.new(1, -56, 0, 16), Position = desc and UDim2.new(0, 12, 0, 5) or UDim2.new(0, 12, 0.5, -8), BackgroundTransparency = 1, Text = lbl, Font = Enum.Font.GothamMedium, TextSize = 11, TextColor3 = C.TX, TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 3})
    if desc then
        N("TextLabel", Cd, {Size = UDim2.new(1, -56, 0, 12), Position = UDim2.new(0, 12, 0, 21), BackgroundTransparency = 1, Text = desc, Font = Enum.Font.Gotham, TextSize = 9, TextColor3 = C.SB, TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 3})
    end
    local Tr = N("TextButton", Cd, {Size = UDim2.fromOffset(36, 19), Position = UDim2.new(1, -44, 0.5, -9.5), BackgroundColor3 = C.OF, Text = "", AutoButtonColor = false, ZIndex = 4})
    N("UICorner", Tr, {CornerRadius = UDim.new(1, 0)})
    local Kn = N("Frame", Tr, {Size = UDim2.fromOffset(13, 13), Position = UDim2.new(0, 3, 0.5, -6.5), BackgroundColor3 = Color3.new(1,1,1), ZIndex = 5})
    N("UICorner", Kn, {CornerRadius = UDim.new(1, 0)})
    Tr.MouseButton1Click:Connect(function()
        S[key] = not S[key]
        local on = S[key]
        TW(Tr, 0.2, {BackgroundColor3 = on and C.ON or C.OF})
        TW(Kn, 0.2, {Position = on and UDim2.new(1, -16, 0.5, -6.5) or UDim2.new(0, 3, 0.5, -6.5)})
        TW(lb, 0.2, {BackgroundColor3 = on and C.A or C.OF})
        if on then
            TW(Cd, 0.1, {BackgroundColor3 = C.P})
            task.delay(0.3, function() TW(Cd, 0.2, {BackgroundColor3 = C.C}) end)
        end
    end)
end

MkSlider = function(par, lbl, mn2, mx, def, cb)
    local Cd = N("Frame", par, {Size = UDim2.new(1, 0, 0, mb and 54 or 46), BackgroundColor3 = C.C, BorderSizePixel = 0, ZIndex = 2})
    N("UICorner", Cd, {CornerRadius = UDim.new(0, 10)})
    N("UIStroke", Cd, {Thickness = 1, Color = C.OF})
    N("TextLabel", Cd, {Size = UDim2.new(1, -54, 0, 16), Position = UDim2.new(0, 12, 0, 6), BackgroundTransparency = 1, Text = lbl, Font = Enum.Font.GothamMedium, TextSize = 11, TextColor3 = C.TX, TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 3})
    local VL = N("TextLabel", Cd, {Size = UDim2.fromOffset(42, 16), Position = UDim2.new(1, -46, 0, 6), BackgroundTransparency = 1, Text = tostring(def), Font = Enum.Font.GothamBold, TextSize = 11, TextColor3 = C.A, TextXAlignment = Enum.TextXAlignment.Right, ZIndex = 3})
    local track = N("Frame", Cd, {Size = UDim2.new(1, -24, 0, 6), Position = UDim2.new(0, 12, 0, 30), BackgroundColor3 = C.OF, BorderSizePixel = 0, ZIndex = 3})
    N("UICorner", track, {CornerRadius = UDim.new(1, 0)})
    local pct = (def - mn2) / (mx - mn2)
    local fill = N("Frame", track, {Size = UDim2.new(pct, 0, 1, 0), BackgroundColor3 = C.A, BorderSizePixel = 0, ZIndex = 4})
    N("UICorner", fill, {CornerRadius = UDim.new(1, 0)})
    local knob = N("Frame", track, {Size = UDim2.fromOffset(16, 16), Position = UDim2.new(pct, -8, 0.5, -8), BackgroundColor3 = Color3.new(1,1,1), ZIndex = 5})
    N("UICorner", knob, {CornerRadius = UDim.new(1, 0)})
    local dr2 = false
    local function upd(x)
        local rel = math.clamp((x - track.AbsolutePosition.X) / math.max(track.AbsoluteSize.X, 1), 0, 1)
        local v = math.round(mn2 + rel * (mx - mn2))
        fill.Size = UDim2.new(rel, 0, 1, 0)
        knob.Position = UDim2.new(rel, -8, 0.5, -8)
        VL.Text = tostring(v)
        cb(v)
    end
    track.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            dr2 = true
            upd(i.Position.X)
        end
    end)
    UI.InputChanged:Connect(function(i)
        if dr2 and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
            upd(i.Position.X)
        end
    end)
    UI.InputEnded:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            dr2 = false
        end
    end)
end

R.Heartbeat:Connect(function()
    local al = (math.sin(tick() * 2.5) + 1) / 2
    MS.Transparency = 0.1 + al * 0.5
    MD.BackgroundColor3 = op and C.A or C.ON
end)

CP = MkTab("Combat", "")
EP = MkTab("ESP", "")
XP = MkTab("Extra", "")
MU = MkTab("Music", "")
MP = MkTab("Misc", "")

TP["Combat"].Visible = true
TB2["Combat"].TextColor3 = C.TX
TW(TB2["Combat"], 0.01, {BackgroundColor3 = C.A})

MkSec(CP, "Automation")
MkTog(CP, "Auto Parry", "AP", "Parries ball automatically")
MkSlider(CP, "Parry Accuracy", 1, 100, 100, function(v) Speed_Divisor_Multiplier = 0.59 + (v - 1) * (3 / 99) end)
MkTog(CP, "Auto Spam", "AS", "Spams parry when targeted")
MkSlider(CP, "Spam Threshold", 1, 3, 1, function(v) ParryThreshold = v end)
MkTog(CP, "Triggerbot", "TB", "Triggerbot on closest player")
MkTog(CP, "Auto Dodge", "AD", "Teleports to avoid ball")

local adFloatGui = N("ScreenGui", pg, {Name = "ENX_ADFloat", ResetOnSpawn = false, DisplayOrder = 11})
local adBtn = N("TextButton", adFloatGui, {Size = UDim2.fromOffset(64, 64), Position = UDim2.new(0, 84, 0.62, 0), BackgroundColor3 = C.A, Text = "", ZIndex = 10, AutoButtonColor = false})
N("UICorner", adBtn, {CornerRadius = UDim.new(1, 0)})
N("UIStroke", adBtn, {Thickness = 2, Color = C.A2, Transparency = 0.4})
N("TextLabel", adBtn, {Size = UDim2.new(1, 0, 0.5, 0), Position = UDim2.new(0, 0, 0, 6), BackgroundTransparency = 1, Text = "↗", Font = Enum.Font.GothamBold, TextSize = 20, TextColor3 = Color3.new(1,1,1), ZIndex = 11})
N("TextLabel", adBtn, {Size = UDim2.new(1, 0, 0.36, 0), Position = UDim2.new(0, 0, 0.62, 0), BackgroundTransparency = 1, Text = "DODGE", Font = Enum.Font.GothamBold, TextSize = 9, TextColor3 = Color3.new(1,1,1), ZIndex = 11})

local adActive = false
local adDrag, adDS, adSP, adDid = false, nil, nil, false

adBtn.InputBegan:Connect(function(i)
    if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
        adDrag = true
        adDS = i.Position
        adSP = adBtn.Position
        adDid = false
    end
end)

adBtn.InputEnded:Connect(function(i)
    if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
        if not adDid then
            adActive = not adActive
            S.AD = adActive
            TW(adBtn, 0.1, {BackgroundColor3 = adActive and Color3.fromRGB(80,220,140) or C.A})
        end
        adDrag = false
    end
end)

UI.InputChanged:Connect(function(i)
    if adDrag and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
        local d = i.Position - adDS
        if not adDid and d.Magnitude > 10 then adDid = true end
        if adDid then
            adBtn.Position = UDim2.new(adSP.X.Scale, adSP.X.Offset + d.X, adSP.Y.Scale, adSP.Y.Offset + d.Y)
        end
    end
end)

MkSec(CP, "Detection")
MkTog(CP, "Infinity Detection", "ID", "Stops parry during Infinity")
MkTog(CP, "Phantom Detection", "PD", "Auto handles Phantom")
MkTog(CP, "Singularity Detect", "SG", "Stops during Singularity cape")

MkSec(CP, "Lobby / Training")
MkTog(CP, "Lobby Auto Parry", "LAP", "Parries in TrainingBalls")
MkTog(CP, "Lobby Auto Spam", "LAS", "Spams in TrainingBalls")

MkSec(CP, "Survival")
MkTog(CP, "Semi Immortal", "SI", "Orbits ball - you move normally")
MkSlider(CP, "Orbit Radius", 4, 150, 20, function(v) orbitR = v end)
MkSlider(CP, "Orbit Height", 0, 100, 5, function(v) orbitH = v end)

MkSec(CP, "Ability Detection")
MkTog(CP, "Infinity Warn", "IW", "Warns Infinity used")
MkTog(CP, "Time Hole Detect", "TD", "Warns Time Hole used")
MkTog(CP, "Death Slash Detect", "DD", "Warns Death Slash used")
MkTog(CP, "Tornado Detect", "TN", "Warns Tornado spawned")
MkTog(CP, "Aerodynamic Detect", "AE2", "Warns + parries Aerodynamic")
MkTog(CP, "Anti Fury", "AF", "Warns Fury used")
MkTog(CP, "Bubble Fury Warn", "BF", "Warns Bubble Fury")
MkTog(CP, "Speed Rush Warn", "SR", "Warns Speed Rush used")

MkSec(EP, "Players")
MkTog(EP, "Player ESP", "PE", "Highlights players")
MkTog(EP, "Distance ESP", "DE", "Shows stud distance")
MkTog(EP, "Rainbow ESP", "RE", "Color cycle highlights")
MkTog(EP, "Staff Detect", "SK", "Highlights staff")

MkSec(EP, "Ball")
MkTog(EP, "Ball ESP", "BE", "Highlights the ball")
MkTog(EP, "Ball Tracer", "BT", "Selection box on ball")

MkSec(XP, "Trackers")
MkTog(XP, "Velocity Tracker", "VT", "Shows ball speed")
MkTog(XP, "Ability ESP", "AE", "Shows ability above players")

MkSec(MP, "Player")
MkSlider(MP, "WalkSpeed", 16, 120, 16, function(v)
    savedWS = v
    local hum = pl.Character and pl.Character:FindFirstChildOfClass("Humanoid")
    if hum then hum.WalkSpeed = v end
end)
MkSlider(MP, "JumpPower", 50, 250, 50, function(v)
    savedJP = v
    local hum = pl.Character and pl.Character:FindFirstChildOfClass("Humanoid")
    if hum then hum.JumpPower = v end
end)

MkSec(MP, "Input Mode")
local pcModeFrame = N("Frame", MP, {Size = UDim2.new(1, 0, 0, 46), BackgroundColor3 = C.C, BorderSizePixel = 0, ZIndex = 2})
N("UICorner", pcModeFrame, {CornerRadius = UDim.new(0, 10)})
N("UIStroke", pcModeFrame, {Thickness = 1, Color = C.OF})
local pcModeLabel = N("TextLabel", pcModeFrame, {Size = UDim2.new(1, -8, 0, 16), Position = UDim2.new(0, 12, 0, 6), BackgroundTransparency = 1, Text = "Mode: Mobile (Touch)", Font = Enum.Font.GothamMedium, TextSize = 11, TextColor3 = C.TX, TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 3})
local pcModeTrack = N("Frame", pcModeFrame, {Size = UDim2.new(1, -24, 0, 6), Position = UDim2.new(0, 12, 0, 30), BackgroundColor3 = C.OF, BorderSizePixel = 0, ZIndex = 3})
N("UICorner", pcModeTrack, {CornerRadius = UDim.new(1, 0)})
local pcModeFill = N("Frame", pcModeTrack, {Size = UDim2.new(0, 0, 1, 0), BackgroundColor3 = C.A, BorderSizePixel = 0, ZIndex = 4})
N("UICorner", pcModeFill, {CornerRadius = UDim.new(1, 0)})
local pcModeKnob = N("Frame", pcModeTrack, {Size = UDim2.fromOffset(16, 16), Position = UDim2.new(0, -8, 0.5, -8), BackgroundColor3 = Color3.new(1,1,1), ZIndex = 5})
N("UICorner", pcModeKnob, {CornerRadius = UDim.new(1, 0)})
local pcDr = false

local function updatePCMode(x)
    local rel = math.clamp((x - pcModeTrack.AbsolutePosition.X) / math.max(pcModeTrack.AbsoluteSize.X, 1), 0, 1)
    pcMode = rel >= 0.5
    local rv = pcMode and 1 or 0
    pcModeFill.Size = UDim2.new(rv, 0, 1, 0)
    pcModeKnob.Position = UDim2.new(rv, -8, 0.5, -8)
    pcModeLabel.Text = pcMode and "Mode: PC (Keyboard)" or "Mode: Mobile (Touch)"
    pcModeLabel.TextColor3 = pcMode and C.A or C.TX
end

pcModeTrack.InputBegan:Connect(function(i)
    if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
        pcDr = true
        updatePCMode(i.Position.X)
    end
end)
UI.InputChanged:Connect(function(i)
    if pcDr and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
        updatePCMode(i.Position.X)
    end
end)
UI.InputEnded:Connect(function(i)
    if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
        pcDr = false
    end
end)

MkSec(MP, "Performance")
MkTog(MP, "FPS Boost", "FP", "Removes effects once")
MkTog(MP, "Full Bright", "FB", "Max brightness")

MkSec(MP, "Render")
MkTog(MP, "No Render (ENX)", "NR", "Destroys Runtime children")
MkTog(MP, "No Render (Alt)", "NR2", "Disables EffectScripts ClientFX")

MkSec(MP, "Sword Changer")
MkTog(MP, "Skin Changer", "SC", "Enables sword skin changing")

local scBox1 = N("TextBox", MP, {Size = UDim2.new(1, 0, 0, 32), BackgroundColor3 = C.C, BorderSizePixel = 0, PlaceholderText = "Sword Model Name...", Font = Enum.Font.Gotham, TextSize = 10, TextColor3 = C.TX, PlaceholderColor3 = C.SB, ClearTextOnFocus = false, ZIndex = 2, TextXAlignment = Enum.TextXAlignment.Left})
N("UICorner", scBox1, {CornerRadius = UDim.new(0, 8)})
N("UIStroke", scBox1, {Thickness = 1, Color = C.OF})
N("UIPadding", scBox1, {PaddingLeft = UDim.new(0, 8)})
scBox1.FocusLost:Connect(function() swordModel = scBox1.Text end)

local scBox2 = N("TextBox", MP, {Size = UDim2.new(1, 0, 0, 32), BackgroundColor3 = C.C, BorderSizePixel = 0, PlaceholderText = "Sword Animation Name...", Font = Enum.Font.Gotham, TextSize = 10, TextColor3 = C.TX, PlaceholderColor3 = C.SB, ClearTextOnFocus = false, ZIndex = 2, TextXAlignment = Enum.TextXAlignment.Left})
N("UICorner", scBox2, {CornerRadius = UDim.new(0, 8)})
N("UIStroke", scBox2, {Thickness = 1, Color = C.OF})
N("UIPadding", scBox2, {PaddingLeft = UDim.new(0, 8)})
scBox2.FocusLost:Connect(function() swordAnimations = scBox2.Text end)

local scBox3 = N("TextBox", MP, {Size = UDim2.new(1, 0, 0, 32), BackgroundColor3 = C.C, BorderSizePixel = 0, PlaceholderText = "Sword FX Name...", Font = Enum.Font.Gotham, TextSize = 10, TextColor3 = C.TX, PlaceholderColor3 = C.SB, ClearTextOnFocus = false, ZIndex = 2, TextXAlignment = Enum.TextXAlignment.Left})
N("UICorner", scBox3, {CornerRadius = UDim.new(0, 8)})
N("UIStroke", scBox3, {Thickness = 1, Color = C.OF})
N("UIPadding", scBox3, {PaddingLeft = UDim.new(0, 8)})
scBox3.FocusLost:Connect(function() swordFX = scBox3.Text end)

local scApply = N("TextButton", MP, {Size = UDim2.new(1, 0, 0, 30), BackgroundColor3 = C.A, Text = "Apply Sword", Font = Enum.Font.GothamBold, TextSize = 11, TextColor3 = Color3.new(1,1,1), AutoButtonColor = false, BorderSizePixel = 0, ZIndex = 2})
N("UICorner", scApply, {CornerRadius = UDim.new(0, 10)})
scApply.MouseButton1Click:Connect(function()
    if not S.SC then
        ShowNotif("Enable Skin Changer first", Color3.fromRGB(255,80,80))
        return
    end
    skinChangerEnabled = true
    swordModel = scBox1.Text
    swordAnimations = scBox2.Text
    swordFX = scBox3.Text
    ShowNotif("Sword applied!", Color3.fromRGB(0,220,140))
end)

MkSec(MP, "Kill Sound")
MkTog(MP, "Kill Sound", "KS", "Plays sound when you kill someone")

local ksSounds = {"Fahhhh", "Very angry", "Leave me alone", "Get over here", "HEHEHE HA", "Head shot", "Lesgoo", "I back", "Fatality", "Pans", "Bruh", "Beautiful girl", "DION TIMMER SHIAWASE"}
local ksIds = {"92076037937225", "96664488756631", "116957716755028", "8643750815", "93779555057888", "84233173598772", "8097518145", "6512108316", "27274429332", "6011094380", "7616380887", "9070284921", "5409360995"}

local ksFrame = N("Frame", MP, {Size = UDim2.new(1, 0, 0, mb and 54 or 46), BackgroundColor3 = C.C, BorderSizePixel = 0, ZIndex = 2})
N("UICorner", ksFrame, {CornerRadius = UDim.new(0, 10)})
N("UIStroke", ksFrame, {Thickness = 1, Color = C.OF})
local ksLabel = N("TextLabel", ksFrame, {Size = UDim2.new(1, -8, 0, 16), Position = UDim2.new(0, 12, 0, 6), BackgroundTransparency = 1, Text = "Sound: Fahhhh", Font = Enum.Font.GothamMedium, TextSize = 11, TextColor3 = C.TX, TextXAlignment = Enum.TextXAlignment.Left})
local ksTrack = N("Frame", ksFrame, {Size = UDim2.new(1, -24, 0, 6), Position = UDim2.new(0, 12, 0, 30), BackgroundColor3 = C.OF, BorderSizePixel = 0})
N("UICorner", ksTrack, {CornerRadius = UDim.new(1, 0)})
local ksFill = N("Frame", ksTrack, {Size = UDim2.new(0, 0, 1, 0), BackgroundColor3 = C.A, BorderSizePixel = 0})
N("UICorner", ksFill, {CornerRadius = UDim.new(1, 0)})
local ksKnob = N("Frame", ksTrack, {Size = UDim2.fromOffset(16, 16), Position = UDim2.new(0, -8, 0.5, -8), BackgroundColor3 = Color3.new(1,1,1)})
N("UICorner", ksKnob, {CornerRadius = UDim.new(1, 0)})
local ksDr = false

local function updateKS(x)
    local rel = math.clamp((x - ksTrack.AbsolutePosition.X) / math.max(ksTrack.AbsoluteSize.X, 1), 0, 1)
    local idx = math.round(1 + rel * (#ksSounds - 1))
    ksFill.Size = UDim2.new((idx - 1) / (#ksSounds - 1), 0, 1, 0)
    ksKnob.Position = UDim2.new((idx - 1) / (#ksSounds - 1), -8, 0.5, -8)
    ksLabel.Text = "Sound: " .. ksSounds[idx]
    KSS_selected = ksIds[idx]
end

ksTrack.InputBegan:Connect(function(i)
    if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
        ksDr = true
        updateKS(i.Position.X)
    end
end)
UI.InputChanged:Connect(function(i)
    if ksDr and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
        updateKS(i.Position.X)
    end
end)
UI.InputEnded:Connect(function(i)
    if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
        ksDr = false
    end
end)

MkSec(MP, "FastFlags")
local ffNames = {"Default (All FFlags)", "Fps Boost", "Lag Fix", "Lag Ball"}

local ffFrame = N("Frame", MP, {Size = UDim2.new(1, 0, 0, 38), BackgroundColor3 = C.C, BorderSizePixel = 0, ZIndex = 2})
N("UICorner", ffFrame, {CornerRadius = UDim.new(0, 10)})
N("UIStroke", ffFrame, {Thickness = 1, Color = C.OF})
local ffLabel = N("TextLabel", ffFrame, {Size = UDim2.new(1, -80, 1, 0), Position = UDim2.new(0, 10, 0, 0), BackgroundTransparency = 1, Text = "Select Profile", Font = Enum.Font.GothamMedium, TextSize = 10, TextColor3 = C.TX, TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 3})
local ffIdx = 1

local function shiftFF(d)
    ffIdx = ((ffIdx - 1 + d) % #ffNames) + 1
    ffLabel.Text = ffNames[ffIdx]
end

local ffL = N("TextButton", ffFrame, {Size = UDim2.fromOffset(28, 24), Position = UDim2.new(1, -62, 0.5, -12), BackgroundColor3 = C.A, Text = "<", Font = Enum.Font.GothamBold, TextSize = 12, TextColor3 = Color3.new(1,1,1), AutoButtonColor = false, ZIndex = 4})
N("UICorner", ffL, {CornerRadius = UDim.new(0, 6)})
ffL.MouseButton1Click:Connect(function() shiftFF(-1) end)

local ffR = N("TextButton", ffFrame, {Size = UDim2.fromOffset(28, 24), Position = UDim2.new(1, -30, 0.5, -12), BackgroundColor3 = C.A, Text = ">", Font = Enum.Font.GothamBold, TextSize = 12, TextColor3 = Color3.new(1,1,1), AutoButtonColor = false, ZIndex = 4})
N("UICorner", ffR, {CornerRadius = UDim.new(0, 6)})
ffR.MouseButton1Click:Connect(function() shiftFF(1) end)

local FFPROFILES = {
    ["Default (All FFlags)"] = {
        DFIntPhysicsEMAInverseSmoothingFactor = "2074",
        FFlagAnimationTrackStepFix = "True",
        DFFlagTextureQualityOverrideEnabled = "True",
        DFIntTextureQualityOverride = "0",
        DFFlagGameNetFixReplicationSkipBug = "True",
        DFFlagCorrectCachePolicySkipRedirectCache = "True",
        FFlagPushFrameTimeToHarmony = "True",
        DFFlagSkipSomePropertiesSkip = "True",
        DFFlagSkipReadDiskCacheRedirects = "True",
        DFFlagSkipSomeProperties = "True",
        FFlagDisablePostFx = "True",
        FIntDebugTextureManagerSkipMips = "4",
        DFIntWaitOnUpdateNetworkLoopEndedMS = "1",
        FFlagAdServiceEnabled = "False",
        DFIntDebugFRMQualityLevelOverride = "3",
        DFIntCSGLevelOfDetailSwitchingDistance = "0",
        DFIntCSGLevelOfDetailSwitchingDistanceL12 = "0",
        DFIntCSGLevelOfDetailSwitchingDistanceL34 = "0",
        DFIntCSGLevelOfDetailSwitchingDistanceL23 = "0",
        FIntRobloxGuiBlurIntensity = "0",
        DFIntDataSenderRate = "2000",
        DFIntRakNetLoopMs = "15",
        DFIntMaxProcessPacketsStepsPerCyclic = "1500",
        DFIntConnectionMTUSize = "1240",
        InterpolationMaxDelayMSec = "0",
        SimSolverResponsiveness = "2147483647",
        DFFlagDebugPerfMode = "True",
        FFlagDebugDisableOptimizedBytecode = "False"
    },
    ["Fps Boost"] = {
        DFFlagTextureQualityOverrideEnabled = "True",
        DFIntTextureQualityOverride = "0",
        DFFlagGameNetFixReplicationSkipBug = "True",
        DFFlagCorrectCachePolicySkipRedirectCache = "True",
        FFlagPushFrameTimeToHarmony = "True",
        DFFlagSkipSomePropertiesSkip = "True",
        DFFlagSkipReadDiskCacheRedirects = "True",
        DFFlagSkipSomeProperties = "True"
    },
    ["Lag Fix"] = {
        FIntDebugTextureManagerSkipMips = "4",
        DFIntWaitOnUpdateNetworkLoopEndedMS = "1",
        FIntFontSizePadding = "2",
        FFlagAdServiceEnabled = "False",
        DFIntDebugFRMQualityLevelOverride = "3",
        DFIntCSGLevelOfDetailSwitchingDistance = "0",
        DFIntCSGLevelOfDetailSwitchingDistanceL12 = "0",
        DFIntCSGLevelOfDetailSwitchingDistanceL34 = "0",
        DFIntCSGLevelOfDetailSwitchingDistanceL23 = "0",
        FIntRobloxGuiBlurIntensity = "0"
    },
    ["Lag Ball"] = {
        DFIntMaxProcessPacketsJobScaling = "2139999999",
        DFIntMaxProcessPacketsStepsAccumulated = "0",
        DFIntMaxProcessPacketsStepsPerCyclic = "2139999999",
        DFIntMaxFrameBufferSize = "4",
        DFIntMaxFramesToSend = "1",
        DFIntMaxAverageFrameDelayExceedFactor = "0",
        DFIntPerformanceControlFrameTimeMax = "1",
        FIntInterpolationAwareTargetTimeLerpHundredth = "100",
        DFIntNumFramesAllowedToBeAboveError = "0",
        DFIntInterpolationMinAssemblyCount = "1",
        DFIntCodecMaxOutgoingFrames = "2139999999",
        DFIntCodecMaxIncomingPackets = "2139999999",
        DFIntClientPacketMaxDelayMs = "1",
        DFIntClientPacketHealthyAllocationPercent = "50",
        DFIntInterpolationDtLimitForLod = "1",
        DFIntParallelAdaptiveInterpolationBatchCount = "1",
        DFIntRakNetClockDriftAdjustmentPerPingMillisecond = "2139999999",
        FIntInterpolationMaxDelayMSec = "1",
        FIntNumFramesToCaptureCallStack = "1"
    }
}

local ffApply = N("TextButton", MP, {Size = UDim2.new(1, 0, 0, 30), BackgroundColor3 = C.A, Text = "Apply FastFlags (Rejoin needed)", Font = Enum.Font.GothamBold, TextSize = 10, TextColor3 = Color3.new(1,1,1), AutoButtonColor = false, BorderSizePixel = 0, ZIndex = 2})
N("UICorner", ffApply, {CornerRadius = UDim.new(0, 10)})
ffApply.MouseButton1Click:Connect(function()
    local sel = ffNames[ffIdx]
    local flags = FFPROFILES[sel]
    if not flags then return end
    local sf = nil
    pcall(function() sf = setfflag end)
    if not sf then
        pcall(function() sf = getgenv().setfflag end)
    end
    if not sf then
        ShowNotif("setfflag unavailable", Color3.fromRGB(255,80,80))
        return
    end
    for k, v in pairs(flags) do
        pcall(function()
            local s = k:gsub("^DFInt", ""):gsub("^DFFlag", ""):gsub("^FFlag", ""):gsub("^FInt", "")
            sf(s, tostring(v))
        end)
    end
    ShowNotif("Applied: " .. sel, Color3.fromRGB(0,220,140))
end)

MkSec(MP, "Hit Sounds")
MkTog(MP, "Hit Sounds", "HS", "Plays sound on parry success")

local hitSoundIds = {
    Medal = "rbxassetid://6607336718",
    Fatality = "rbxassetid://6607113255",
    Skeet = "rbxassetid://6607204501",
    Switches = "rbxassetid://6607173363",
    Bubble = "rbxassetid://6534947588",
    Laser = "rbxassetid://7837461331",
    Steve = "rbxassetid://4965083997",
    Bat = "rbxassetid://3333907347",
    Saber = "rbxassetid://8415678813"
}

local hsfolder = Instance.new("Folder")
hsfolder.Name = "ENX_Sounds"
hsfolder.Parent = workspace
hitSound = Instance.new("Sound", hsfolder)
hitSound.Volume = 6
hitSound.SoundId = hitSoundIds.Medal

local hitSoundNames = {"Medal", "Fatality", "Skeet", "Switches", "Bubble", "Laser", "Steve", "Bat", "Saber"}

local hsFrame = N("Frame", MP, {Size = UDim2.new(1, 0, 0, mb and 54 or 46), BackgroundColor3 = C.C, BorderSizePixel = 0, ZIndex = 2})
N("UICorner", hsFrame, {CornerRadius = UDim.new(0, 10)})
N("UIStroke", hsFrame, {Thickness = 1, Color = C.OF})
local hsLabel = N("TextLabel", hsFrame, {Size = UDim2.new(1, -8, 0, 16), Position = UDim2.new(0, 12, 0, 6), BackgroundTransparency = 1, Text = "Sound: Medal", Font = Enum.Font.GothamMedium, TextSize = 11, TextColor3 = C.TX, TextXAlignment = Enum.TextXAlignment.Left})
local hsTrack = N("Frame", hsFrame, {Size = UDim2.new(1, -24, 0, 6), Position = UDim2.new(0, 12, 0, 30), BackgroundColor3 = C.OF, BorderSizePixel = 0})
N("UICorner", hsTrack, {CornerRadius = UDim.new(1, 0)})
local hsFill = N("Frame", hsTrack, {Size = UDim2.new(0, 0, 1, 0), BackgroundColor3 = C.A, BorderSizePixel = 0})
N("UICorner", hsFill, {CornerRadius = UDim.new(1, 0)})
local hsKnob = N("Frame", hsTrack, {Size = UDim2.fromOffset(16, 16), Position = UDim2.new(0, -8, 0.5, -8), BackgroundColor3 = Color3.new(1,1,1)})
N("UICorner", hsKnob, {CornerRadius = UDim.new(1, 0)})
local hsDr = false

local function updateHS(x)
    local rel = math.clamp((x - hsTrack.AbsolutePosition.X) / math.max(hsTrack.AbsoluteSize.X, 1), 0, 1)
    local idx = math.round(1 + rel * (#hitSoundNames - 1))
    hsFill.Size = UDim2.new((idx - 1) / (#hitSoundNames - 1), 0, 1, 0)
    hsKnob.Position = UDim2.new((idx - 1) / (#hitSoundNames - 1), -8, 0.5, -8)
    local sn = hitSoundNames[idx]
    hsLabel.Text = "Sound: " .. sn
    hitSound.SoundId = hitSoundIds[sn] or hitSoundIds.Medal
end

hsTrack.InputBegan:Connect(function(i)
    if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
        hsDr = true
        updateHS(i.Position.X)
    end
end)
UI.InputChanged:Connect(function(i)
    if hsDr and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
        updateHS(i.Position.X)
    end
end)
UI.InputEnded:Connect(function(i)
    if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
        hsDr = false
    end
end)

MkSec(MP, "Credits")
local cred = N("Frame", MP, {Size = UDim2.new(1, 0, 0, 48), BackgroundColor3 = C.C, BorderSizePixel = 0, ZIndex = 2})
N("UICorner", cred, {CornerRadius = UDim.new(0, 10)})
N("UIStroke", cred, {Thickness = 1, Color = C.OF})
N("TextLabel", cred, {Size = UDim2.new(1, 0, 0, 22), Position = UDim2.new(0, 0, 0, 6), BackgroundTransparency = 1, Text = "EclipseNexus v2.0", Font = Enum.Font.GothamBold, TextSize = 14, TextColor3 = C.A, TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 3})
N("TextLabel", cred, {Size = UDim2.new(1, 0, 0, 16), Position = UDim2.new(0, 0, 0, 28), BackgroundTransparency = 1, Text = "BladeBall Script", Font = Enum.Font.Gotham, TextSize = 11, TextColor3 = C.TX, TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 3})

local function DragBtn(h, tg)
    local dr, ds, sp, di = false
    h.InputBegan:Connect(function(n)
        if n.UserInputType == Enum.UserInputType.MouseButton1 or n.UserInputType == Enum.UserInputType.Touch then
            dr, ds, sp = true, n.Position, tg.Position
            n.Changed:Connect(function()
                if n.UserInputState == Enum.UserInputState.End then dr = false end
            end)
        end
    end)
    h.InputChanged:Connect(function(n)
        if n.UserInputType == Enum.UserInputType.MouseMovement or n.UserInputType == Enum.UserInputType.Touch then
            di = n
        end
    end)
    UI.InputChanged:Connect(function(n)
        if n == di and dr then
            local d = n.Position - ds
            tg.Position = UDim2.new(sp.X.Scale, sp.X.Offset + d.X, sp.Y.Scale, sp.Y.Offset + d.Y)
        end
    end)
end

SB = N("TextButton", SG2, {Size = UDim2.fromOffset(64, 64), Position = UDim2.new(0, 10, 0.62, 0), BackgroundColor3 = C.A, Text = "", ZIndex = 10})
N("UICorner", SB, {CornerRadius = UDim.new(1, 0)})
RI(SB, "BackgroundColor3", "A")
N("UIStroke", SB, {Thickness = 2, Color = C.A2, Transparency = 0.4})
N("TextLabel", SB, {Size = UDim2.new(1, 0, 0.5, 0), Position = UDim2.new(0, 0, 0, 6), BackgroundTransparency = 1, Text = ">>", Font = Enum.Font.GothamBold, TextSize = 16, TextColor3 = Color3.new(1,1,1), ZIndex = 11})
N("TextLabel", SB, {Size = UDim2.new(1, 0, 0.36, 0), Position = UDim2.new(0, 0, 0.62, 0), BackgroundTransparency = 1, Text = "SPAM", Font = Enum.Font.GothamBold, TextSize = 9, TextColor3 = Color3.new(1,1,1), ZIndex = 11})
DragBtn(SB, SB)

VTUI = N("Frame", SG2, {Size = UDim2.fromOffset(190, 100), Position = UDim2.new(0.5, -95, 0, 56), BackgroundColor3 = C.P, BorderSizePixel = 0, Visible = false, ZIndex = 15})
N("UICorner", VTUI, {CornerRadius = UDim.new(0, 12)})
local VTUIS = N("UIStroke", VTUI, {Thickness = 1.5, Color = C.A, Transparency = 0.3})
DragBtn(VTUI, VTUI)

local VTH = N("Frame", VTUI, {Size = UDim2.new(1, 0, 0, 22), BackgroundColor3 = C.C, BorderSizePixel = 0, ZIndex = 16})
N("UICorner", VTH, {CornerRadius = UDim.new(0, 12)})
local VTHF = N("Frame", VTH, {Size = UDim2.new(1, 0, 0, 12), Position = UDim2.new(0, 0, 1, -12), BackgroundColor3 = C.C, BorderSizePixel = 0, ZIndex = 17})
N("TextLabel", VTH, {Size = UDim2.new(1, 0, 1, 0), BackgroundTransparency = 1, Text = "Ball Velocity", Font = Enum.Font.GothamBold, TextSize = 11, TextColor3 = C.A, TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 17})

VTL1 = N("TextLabel", VTUI, {Size = UDim2.new(1, -8, 0, 20), Position = UDim2.new(0, 4, 0, 24), BackgroundTransparency = 1, Text = "-- st/s", Font = Enum.Font.GothamBold, TextSize = 15, TextColor3 = C.TX, TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 16})
VTL3 = N("TextLabel", VTUI, {Size = UDim2.new(1, -8, 0, 16), Position = UDim2.new(0, 4, 0, 44), BackgroundTransparency = 1, Text = "Peak: 0 st/s", Font = Enum.Font.GothamBold, TextSize = 11, TextColor3 = C.ON, TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 16})
VTL2 = N("TextLabel", VTUI, {Size = UDim2.new(1, -8, 0, 14), Position = UDim2.new(0, 4, 0, 62), BackgroundTransparency = 1, Text = "X:0 Y:0 Z:0", Font = Enum.Font.Gotham, TextSize = 10, TextColor3 = C.SB, TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 16})

VTUI_REF.frame = VTUI
VTUI_REF.stroke = VTUIS
VTUI_REF.header = VTH
VTUI_REF.hfill = VTHF

NOTIF = N("Frame", SG2, {Size = UDim2.fromOffset(290, 44), Position = UDim2.new(0.5, -145, 0, 60), BackgroundColor3 = Color3.fromRGB(12,12,20), BorderSizePixel = 0, Visible = false, ZIndex = 30})
N("UICorner", NOTIF, {CornerRadius = UDim.new(0, 10)})
N("UIStroke", NOTIF, {Thickness = 1.5, Color = Color3.fromRGB(255,80,80)})
NOTIF_TL = N("TextLabel", NOTIF, {Size = UDim2.new(1, -10, 1, 0), Position = UDim2.new(0, 8, 0, 0), BackgroundTransparency = 1, Text = "", Font = Enum.Font.GothamBold, TextSize = 13, TextColor3 = Color3.fromRGB(255,80,80), TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 31})

ShowNotif = function(txt, col)
    pcall(function()
        NOTIF_TL.Text = "! " .. txt
        NOTIF_TL.TextColor3 = col or Color3.fromRGB(255,80,80)
        local st2 = NOTIF:FindFirstChildOfClass("UIStroke")
        if st2 then st2.Color = col or Color3.fromRGB(255,80,80) end
        NOTIF.Visible = true
        task.delay(3, function() NOTIF.Visible = false end)
    end)
end

UI.InputBegan:Connect(function(i)
    if i.KeyCode == Enum.KeyCode.RightShift then
        OB:FireButton1()
    end
end)

isStaff = function(p)
    local n = p.Name:lower()
    for _, s in ipairs({"mods", "admin", "staff", "owner", "developer", "dev", "moderator"}) do
        if n:find(s) then return true end
    end
    local ok, rank = pcall(function() return p:GetRankInGroup(2868472) end)
    if ok and rank >= 200 then return true end
    return false
end

Alive = workspace:FindFirstChild("Alive")
Runtime = workspace:FindFirstChild("Runtime")

task.spawn(function()
    pcall(function() Balls = workspace:WaitForChild("Balls", 10) end)
end)

task.spawn(function()
    pcall(function() TrainingBalls = workspace:WaitForChild("TrainingBalls", 10) end)
end)

local function isValidArgs(a)
    return #a == 7 and type(a[2]) == "string" and type(a[3]) == "number" and typeof(a[4]) == "CFrame" and type(a[5]) == "table" and type(a[6]) == "table" and type(a[7]) == "boolean"
end

task.spawn(function()
    pcall(function()
        local mt2 = getrawmetatable(game)
        if mt2 then
            local old2 = mt2.__index
            setreadonly(mt2, false)
            mt2.__index = newcclosure(function(self, key)
                if key == "FireServer" and self:IsA("RemoteEvent") then
                    return newcclosure(function(_, ...)
                        local a = {...}
                        if isValidArgs(a) or self.Name:find("Parry") then
                            revertedRemotes[self] = a
                            if not hasData then
                                hasData = true
                                pcall(function() ST.Text = "Ready! (hook)" end)
                            end
                        end
                        return old2(self, key)(self, ...)
                    end)
                end
                return old2(self, key)
            end)
            setreadonly(mt2, true)
        end
    end)
end)

task.spawn(function()
    task.wait(1)
    pcall(function()
        local t2
        for _, v in filtergc("table", {}) do
            local valid = true
            for i = 0, 1 do
                local r = rawget(v, i)
                if not (typeof(r) == "Instance" and r:IsA("RemoteEvent")) then
                    valid = false
                    break
                end
            end
            if valid then
                t2 = v
                break
            end
        end
        if not t2 then return end
        local uuids = {}
        for _, v in pairs(t2) do
            if type(v) == "string" and v:match("%x%x%x%x%x%x%x%x%-%x%x%x%x%-%x%x%x%x%-%x%x%x%x%-%x%x%x%x%x%x%x%x%x%x%x%x") then
                table.insert(uuids, v)
            end
        end
        local info = rawget(t2, 3)
        if not info then return end
        local selector = rawget(info, 3)
        local uuid = uuids[selector]
        local hash = rawget(info, 2)
        if not uuid or not hash then return end
        local remoteyield = Instance.new("BindableEvent")
        local waxfs = (game:FindFirstChildWhichIsA("RemoteEvent", true) or Instance.new("RemoteEvent")).FireServer
        local fs
        fs = hookfunction(waxfs, function(self, ...)
            if not checkcaller() and self:IsA("RemoteEvent") and select(1, ...) == uuid and select(2, ...) == hash then
                hookfunction(waxfs, fs)
                remoteyield:Fire(self)
            end
            return fs(self, ...)
        end)
        local rf = remoteyield.Event:Wait()
        hasData = true
        revertedRemotes[rf] = {uuid, hash, 0.5, workspace.CurrentCamera.CFrame, {}, {workspace.CurrentCamera.ViewportSize.X / 2, workspace.CurrentCamera.ViewportSize.Y / 2}, false}
        pcall(function() ST.Text = "Ready! (filtergc)" end)
    end)
end)

task.spawn(function()
    local ok, b = pcall(function() return workspace:WaitForChild("Balls", 10) end)
    if ok and b then Balls = b end
    if Balls then
        Balls.ChildAdded:Connect(function()
            Sys.__parried = false
            Sys.__antidot_parried = false
        end)
        Balls.ChildRemoved:Connect(function()
            Parries = 0
            Sys.__parried = false
            Sys.__antidot_parried = false
            Phantom = false
            peakSpeed = 0
            if VTL3 then VTL3.Text = "Peak: 0 st/s" end
        end)
    end
end)

local function riverGetBall()
    local balls = workspace:FindFirstChild("Balls")
    if not balls then return nil end
    for _, ball in pairs(balls:GetChildren()) do
        if ball:GetAttribute("realBall") then
            ball.CanCollide = false
            return ball
        end
    end
    return nil
end

local function riverGetBalls()
    local t = {}
    local balls = workspace:FindFirstChild("Balls")
    if not balls then return t end
    for _, ball in pairs(balls:GetChildren()) do
        if ball:GetAttribute("realBall") then
            ball.CanCollide = false
            table.insert(t, ball)
        end
    end
    return t
end

local lastClosestCheck = 0

local function riverGetClosest()
    local now = tick()
    if now - lastClosestCheck < 0.1 then return Closest_Entity end
    lastClosestCheck = now
    local maxD = math.huge
    local found = nil
    if not Alive then return nil end
    for _, e in pairs(Alive:GetChildren()) do
        if e ~= pl.Character and e.PrimaryPart then
            local d = pl:DistanceFromCharacter(e.PrimaryPart.Position)
            if d < maxD then
                maxD = d
                found = e
            end
        end
    end
    Closest_Entity = found
    return found
end

local function riverGetClosestToCursor()
    if not pl.Character or not pl.Character:FindFirstChild("HumanoidRootPart") then return nil end
    local closest = nil
    local minDot = -math.huge
    local cam = workspace.CurrentCamera
    if not Alive then return nil end
    local ok, ml = pcall(function() return UI:GetMouseLocation() end)
    if not ok then return nil end
    local ray = cam:ScreenPointToRay(ml.X, ml.Y)
    local ptr = CFrame.lookAt(ray.Origin, ray.Origin + ray.Direction)
    for _, player in pairs(Alive:GetChildren()) do
        if player == pl.Character then continue end
        if not player:FindFirstChild("HumanoidRootPart") then continue end
        local dir = (player.HumanoidRootPart.Position - cam.CFrame.Position).Unit
        local dot = ptr.LookVector:Dot(dir)
        if dot > minDot then
            minDot = dot
            closest = player
        end
    end
    return closest
end

local function riverGetCFrame()
    local cam = workspace.CurrentCamera
    local root = pl.Character and pl.Character:FindFirstChild("HumanoidRootPart")
    if not root then return cam.CFrame end
    local closest = riverGetClosestToCursor()
    local targetPart = closest and closest:FindFirstChild("HumanoidRootPart")
    local targetPos = targetPart and targetPart.Position or (root.Position + cam.CFrame.LookVector * 100)
    local funcs = {
        function() return cam.CFrame end,
        function()
            local dir = (targetPos - root.Position).Unit
            local offset
            local attempts = 0
            repeat
                offset = Vector3.new(math.random(-4000, 4000), math.random(-4000, 4000), math.random(-4000, 4000))
                local cd = (targetPos + offset - root.Position).Unit
                local dot = dir:Dot(cd)
                attempts = attempts + 1
            until dot < 0.95 or attempts > 10
            return CFrame.new(root.Position, targetPos + offset)
        end,
        function() return CFrame.new(root.Position, targetPos + Vector3.new(0, 5, 0)) end,
        function()
            local dir = (root.Position - targetPos).Unit
            local bp = root.Position + dir * 10000 + Vector3.new(0, 1000, 0)
            return CFrame.new(cam.CFrame.Position, bp)
        end,
        function() return CFrame.new(root.Position, targetPos + Vector3.new(0, -9e18, 0)) end,
        function() return CFrame.new(root.Position, targetPos + Vector3.new(0, 9e18, 0)) end
    }
    return funcs[Sys.__curve_mode]()
end

local function riverPlayAnim()
    if not Sys.__play_animation then return end
    local char = pl.Character
    if not char then return end
    local hum = char:FindFirstChildOfClass("Humanoid")
    local anim = hum and hum:FindFirstChildOfClass("Animator")
    if not hum or not anim then return end
    local swordName = skinChangerEnabled and swordAnimations ~= "" and swordAnimations or char:GetAttribute("CurrentlyEquippedSword")
    if not swordName then return end
    pcall(function()
        local swordAPI = RS.Shared.SwordAPI.Collection
        local parryAnim = swordAPI.Default:FindFirstChild("GrabParry")
        if not parryAnim then return end
        local swordData = RS.Shared.ReplicatedInstances.Swords.GetSword:Invoke(swordName)
        if not swordData or not swordData.AnimationType then return end
        for _, obj in pairs(swordAPI:GetChildren()) do
            if obj.Name == swordData.AnimationType then
                local at = obj:FindFirstChild("GrabParry") and "GrabParry" or "Grab"
                if obj:FindFirstChild(at) then parryAnim = obj[at] end
            end
        end
        if Sys.__grab_animation and Sys.__grab_animation.IsPlaying then Sys.__grab_animation:Stop() end
        Sys.__grab_animation = anim:LoadAnimation(parryAnim)
        Sys.__grab_animation.Priority = Enum.AnimationPriority.Action4
        Sys.__grab_animation:Play()
    end)
end

doFire = function()
    if not pl.Character then return end
    local cam = workspace.CurrentCamera
    local ok, mouse = pcall(function() return UI:GetMouseLocation() end)
    if not ok then return end
    local vec2 = {(mb and not pcMode) and cam.ViewportSize.X / 2 or mouse.X, (mb and not pcMode) and cam.ViewportSize.Y / 2 or mouse.Y}
    local eventData = {}
    if Alive then
        for _, e in pairs(Alive:GetChildren()) do
            if e.PrimaryPart then
                pcall(function()
                    local sp = cam:WorldToScreenPoint(e.PrimaryPart.Position)
                    eventData[e.Name] = sp
                end)
            end
        end
    end
    local cf = riverGetCFrame()
    if not Sys.__first_parry_done then
        pcall(function()
            for _, conn in pairs(getconnections(pl.PlayerGui.Hotbar.Block.Activated)) do
                conn:Fire()
            end
        end)
        Sys.__first_parry_done = true
        serverFireCounter = serverFireCounter + 1
        return
    end
    for rem, args in pairs(revertedRemotes) do
        local modified = {args[1], args[2], args[3], cf, eventData, vec2, args[7]}
        pcall(function()
            if rem:IsA("RemoteEvent") then
                rem:FireServer(table.unpack(modified))
            elseif rem:IsA("RemoteFunction") then
                rem:InvokeServer(table.unpack(modified))
            end
        end)
    end
    serverFireCounter = serverFireCounter + 1
    if Parries < 10000 then
        Parries = Parries + 1
        task.delay(0.5, function()
            if Parries > 0 then Parries = Parries - 1 end
        end)
    end
end

local ms_conn = nil

SB.MouseButton1Click:Connect(function()
    if not hasData then
        ST.Text = "Capturing...wait!"
        return
    end
    ms = not ms
    local function TW2(o, t, p)
        pcall(function() TS:Create(o, TweenInfo.new(t, Enum.EasingStyle.Quart), p):Play() end)
    end
    if ms then
        TW2(SB, 0.1, {BackgroundColor3 = Color3.fromRGB(138,43,226)})
        ms_conn = R.Stepped:Connect(function() doFire() end)
    else
        TW2(SB, 0.1, {BackgroundColor3 = C.A})
        if ms_conn then ms_conn:Disconnect() ms_conn = nil end
    end
end)

R.Heartbeat:Connect(function()
    if not ms then SB.BackgroundColor3 = C.A end
end)

pcall(function()
    RS.Remotes.ParrySuccess.OnClientEvent:Connect(function()
        if S.HS and hitSound then pcall(function() hitSound:Play() end) end
    end)
end)

pcall(function()
    RS.Remotes.InfinityBall.OnClientEvent:Connect(function(a, b)
        Infinity = b or false
        Sys.__infinity_active = b or false
    end)
end)

pcall(function()
    RS.Remotes.DeathBall.OnClientEvent:Connect(function(c, d)
        Sys.__deathslash_active = d or false
    end)
end)

local det = {__last_warping = tick(), __lerp_radians = 0, __curving = tick(), __aerodynamic_time = tick()}

local function linearPredict(a, b, t)
    return a + (b - a) * t
end

local function riverIsCurved()
    local ball = nil
    if Balls then
        for _, v in ipairs(Balls:GetChildren()) do
            if v:GetAttribute("realBall") then
                ball = v
                break
            end
        end
    end
    if not ball then return false end
    local zoomies = ball:FindFirstChild("zoomies")
    if not zoomies then return false end
    local velocity = zoomies.VectorVelocity
    local speed = velocity.Magnitude
    if speed < 1 then return false end
    local ballDir = velocity.Unit
    local char = pl.Character
    if not char or not char.PrimaryPart then return false end
    local pos = char.PrimaryPart.Position
    local direction = (pos - ball.Position).Unit
    local dot = direction:Dot(ballDir)
    local ping = Stats.Network.ServerStatsItem["Data Ping"]:GetValue() / 1000
    local distance = (pos - ball.Position).Magnitude
    local reachTime = distance / speed - ping
    local dotThreshold = math.clamp(0.55 - (ping * 0.75), -1, 0.45)
    local speedThreshold = math.min(speed / 100, 45)
    local ballDistThreshold = 15 - math.min(distance / 1000, 15) + speedThreshold
    local clampedDot = math.clamp(dot, -1, 1)
    local radians = math.asin(clampedDot)
    det.__lerp_radians = linearPredict(det.__lerp_radians, radians, 0.85)
    if det.__lerp_radians < 0.016 then det.__last_warping = tick() end
    if distance < (ballDistThreshold * 0.85) then return false end
    if (tick() - det.__last_warping) < (reachTime / 1.4) then return true end
    if (tick() - det.__curving) < (reachTime / 1.1) then return true end
    return dot < dotThreshold
end

pcall(function()
    RS.Packages._Index["sleitnick_net@0.1.0"].net["RE/TimeHoleActivate"].OnClientEvent:Connect(function(...)
        local args = {...}
        local p = args[1]
        if p == pl or p == pl.Name or (p and p.Name == pl.Name) then
            Sys.__timehole_active = true
        end
    end)
end)

pcall(function()
    RS.Packages._Index["sleitnick_net@0.1.0"].net["RE/TimeHoleDeactivate"].OnClientEvent:Connect(function()
        Sys.__timehole_active = false
    end)
end)

pcall(function()
    RS.Remotes.ParrySuccessAll.OnClientEvent:Connect(function(_, root)
        if root and root.Parent and root.Parent ~= pl.Character then
            if not Alive or root.Parent.Parent ~= Alive then return end
        end
        local ball = nil
        if Balls then
            for _, v in ipairs(Balls:GetChildren()) do
                if v:GetAttribute("realBall") then
                    ball = v
                    break
                end
            end
        end
        if not ball then return end
        local zoomies = ball:FindFirstChild("zoomies")
        if zoomies then
            local speed = zoomies.VectorVelocity.Magnitude
            local dir = (pl.Character.PrimaryPart.Position - ball.Position).Unit
            local dot = dir:Dot(zoomies.VectorVelocity.Unit)
            if dot < 0.5 then det.__curving = tick() end
        end
    end)
end)

local apConn = nil

R.Heartbeat:Connect(function()
    if S.AP and not apConn then
        apConn = R.RenderStepped:Connect(function()
            if not S.AP or not pl.Character or not pl.Character.PrimaryPart then return end
            local balls = riverGetBalls()
            local trainingBall = nil
            if TrainingBalls then
                for _, v in ipairs(TrainingBalls:GetChildren()) do
                    if v:GetAttribute("realBall") then
                        trainingBall = v
                        break
                    end
                end
            end
            for _, ball in ipairs(balls) do
                if not ball then continue end
                local zoomies = ball:FindFirstChild("zoomies")
                if not zoomies then continue end
                ball:GetAttributeChangedSignal("target"):Once(function()
                    Sys.__parried = false
                    Sys.__antidot_parried = false
                end)
                if Sys.__parried then continue end
                local ballTarget = ball:GetAttribute("target")
                local velocity = zoomies.VectorVelocity
                local distance = (pl.Character.PrimaryPart.Position - ball.Position).Magnitude
                local ping = Stats.Network.ServerStatsItem["Data Ping"]:GetValue()
                local pingThreshold = math.clamp(ping / 10, 5, 17)
                local speed = velocity.Magnitude
                if speed <= 0 then continue end
                local cappedSpeedDiff = math.min(math.max(speed - 9.5, 0), 650)
                local speedDivisor = (2.5 + cappedSpeedDiff * 0.002) * Speed_Divisor_Multiplier
                local parryAccuracy = pingThreshold + math.max(speed / speedDivisor, 9.5)
                parryAccuracy = parryAccuracy + math.clamp(ping / 1000 * speed, 0, 30)
                if speed > 1500 then
                    local m = 1 + (speed - 1500) / 1000
                    parryAccuracy = parryAccuracy * math.clamp(m, 1.5, 3.5)
                end
                local dirToPlayer = (pl.Character.PrimaryPart.Position - ball.Position).Unit
                local dotToPlayer = dirToPlayer:Dot(velocity.Unit)
                if ballTarget ~= pl.Name and dotToPlayer < 0.1 then continue end
                if ball:FindFirstChild("AeroDynamicSlashVFX") then
                    pcall(function() ball.AeroDynamicSlashVFX:Destroy() end)
                    det.__aerodynamic_time = tick()
                end
                if Runtime and Runtime:FindFirstChild("Tornado") then
                    local tornadoTime = (Runtime.Tornado:GetAttribute("TornadoTime") or 1) + 0.314159
                    if (tick() - Sys.__tornado_time) < tornadoTime then continue end
                end
                if riverIsCurved() and ballTarget == pl.Name then continue end
                if ball:FindFirstChild("ComboCounter") then continue end
                if pl.Character.PrimaryPart:FindFirstChild("SingularityCape") then continue end
                if S.ID and Sys.__infinity_active then continue end
                local closestPlayer = riverGetClosest()
                if closestPlayer and not Sys.__antidot_parried then
                    local playerDist = (pl.Character.PrimaryPart.Position - closestPlayer.PrimaryPart.Position).Magnitude
                    if playerDist <= 30 and dotToPlayer > 0.75 then
                        if ballTarget == pl.Name and distance <= 30 then
                            doFire()
                            Sys.__parried = true
                            Sys.__antidot_parried = true
                        end
                    end
                end
                if S.TB and closestPlayer and closestPlayer.Name == ballTarget then
                    local distToClosest = (pl.Character.PrimaryPart.Position - closestPlayer.PrimaryPart.Position).Magnitude
                    if distToClosest <= 35 and ballTarget == pl.Name and distance <= parryAccuracy then
                        doFire()
                        Sys.__parried = true
                    end
                else
                    if ballTarget == pl.Name and distance <= parryAccuracy then
                        if Parries > 7 then continue end
                        Parries = Parries + 1
                        task.delay(0.5, function()
                            if Parries > 0 then Parries = Parries - 1 end
                        end)
                        doFire()
                        Sys.__parried = true
                    end
                end
                if Sys.__parried then
                    local t0 = tick()
                    repeat R.RenderStepped:Wait() until (tick() - t0) >= 1 or not Sys.__parried
                    Sys.__parried = false
                    Sys.__antidot_parried = false
                end
            end
            if trainingBall and S.LAP then
                local tz = trainingBall:FindFirstChild("zoomies")
                if tz then
                    trainingBall:GetAttributeChangedSignal("target"):Once(function()
                        Sys.__training_parried = false
                    end)
                    if not Sys.__training_parried then
                        local bt = trainingBall:GetAttribute("target")
                        local spd = tz.VectorVelocity.Magnitude
                        local dist = pl:DistanceFromCharacter(trainingBall.Position)
                        local ping2 = Stats.Network.ServerStatsItem["Data Ping"]:GetValue()
                        local pingT = math.clamp(ping2 / 10, 5, 17)
                        local csd = math.min(math.max(spd - 9.5, 0), 650)
                        local spdDiv = (2.5 + csd * 0.002) * Speed_Divisor_Multiplier
                        local pa = pingT + math.max(spd / spdDiv, 9.5) + math.clamp(ping2 / 1000 * spd, 0, 30)
                        if spd > 1500 then
                            local m2 = 1 + (spd - 1500) / 1000
                            pa = pa * math.clamp(m2, 1.5, 3.5)
                        end
                        if bt == pl.Name and dist <= pa then
                            doFire()
                            Sys.__training_parried = true
                            local t0 = tick()
                            repeat R.RenderStepped:Wait() until (tick() - t0) >= 1 or not Sys.__training_parried
                            Sys.__training_parried = false
                        end
                    end
                end
            end
        end)
    elseif not S.AP and apConn then
        apConn:Disconnect()
        apConn = nil
    end
end)

local asConn = nil

R.Heartbeat:Connect(function()
    if S.AS and not asConn then
        asConn = R.RenderStepped:Connect(function()
            if not S.AS then return end
            local char = pl.Character
            if not char or not char.PrimaryPart then return end
            if not Alive or char.Parent ~= Alive then return end
            local now = tick()
            if now - (Sys.__last_spam_check or 0) < 0.008 then return end
            Sys.__last_spam_check = now
            local ball = nil
            if Balls then
                for _, v in ipairs(Balls:GetChildren()) do
                    if v:GetAttribute("realBall") then
                        ball = v
                        break
                    end
                end
            end
            if not ball then return end
            local zoomies = ball:FindFirstChild("zoomies")
            if not zoomies then return end
            if now - (Sys.__target_check_time or 0) > 0.1 then
                Sys.__target_check_time = now
                local ce = riverGetClosest()
                if ce then
                    if Sys.__spam_target then
                        if not Sys.__spam_target.Parent or not Sys.__spam_target:FindFirstChild("Humanoid") or Sys.__spam_target.Humanoid.Health <= 0 then
                            Sys.__spam_target = nil
                        end
                    end
                    if not Sys.__spam_target or (now - (Sys.__spam_target_time or 0) > 1) then
                        Sys.__spam_target = ce
                        Sys.__spam_target_time = now
                    end
                end
            end
            local ballTarget = ball:GetAttribute("target")
            if not ballTarget then return end
            local vel = ball.AssemblyLinearVelocity or Vector3.zero
            local speed = vel.Magnitude
            if speed < 5 then return end
            local hr = char.PrimaryPart
            local dir = (hr.Position - ball.Position).Unit
            local dot = dir:Dot(vel.Unit)
            local ping = Stats.Network.ServerStatsItem["Data Ping"]:GetValue()
            local pingThreshold = math.clamp(ping / 10, 1, 16)
            local maxSpamDist = pingThreshold + math.min(speed / 6, 95)
            local maxSpeed = 5 - math.min(speed / 5, 5)
            local maxDot = math.clamp(dot, -1, 0) * maxSpeed
            local spamAcc = maxSpamDist - maxDot
            local entity = Closest_Entity
            if not entity or not entity.PrimaryPart then return end
            local entityDist = (hr.Position - entity.PrimaryPart.Position).Magnitude
            local ballDist = pl:DistanceFromCharacter(ball.Position)
            local targetDist = pl:DistanceFromCharacter(entity.PrimaryPart.Position)
            if entityDist > maxSpamDist then return end
            if ballDist > spamAcc then return end
            if targetDist > maxSpamDist then return end
            if ballTarget == pl.Name and targetDist > 30 and ballDist > 30 then return end
            if char:GetAttribute("Pulsed") then return end
            local spamTarget = Sys.__spam_target
            if not spamTarget then return end
            if ballTarget ~= spamTarget.Name and ballTarget ~= pl.Name then return end
            if targetDist <= spamAcc and ballDist <= spamAcc then
                if Parries > ParryThreshold then
                    doFire()
                    if Sys.__play_animation then riverPlayAnim() end
                end
            end
        end)
    elseif not S.AS and asConn then
        asConn:Disconnect()
        asConn = nil
    end
end)

R.Heartbeat:Connect(function()
    pcall(function()
        if not S.LAS then return end
        local now = tick()
        if (now - lobbySpamDB) < 0.06 then return end
        if not TrainingBalls then TrainingBalls = workspace:FindFirstChild("TrainingBalls") end
        if not TrainingBalls then return end
        local ball = nil
        for _, v in ipairs(TrainingBalls:GetChildren()) do
            if v:GetAttribute("realBall") then
                ball = v
                break
            end
        end
        if not ball then return end
        local tz = ball:FindFirstChild("zoomies")
        if not tz then return end
        local spd = tz.VectorVelocity.Magnitude
        local dist = pl:DistanceFromCharacter(ball.Position)
        local ping = Stats.Network.ServerStatsItem["Data Ping"]:GetValue() / 10
        local pingT = math.clamp(ping / 10, 5, 17)
        local csd = math.min(math.max(spd - 9.5, 0), 650)
        local spdDiv = (2.5 + csd * 0.002) * Speed_Divisor_Multiplier
        local acc = pingT + math.max(spd / spdDiv, 9.5)
        if spd > 2000 then acc = acc * 2.0 end
        local bt = ball:GetAttribute("target")
        if bt == pl.Name and dist <= acc then
            lobbySpamDB = now
            doFire()
        end
    end)
end)

local function DetectAbility(key, abilityName, color)
    pcall(function()
        if not S[key] then return end
        for _, p in ipairs(P:GetPlayers()) do
            if p ~= pl and p.Character then
                local now = os.clock()
                local dbKey = key .. p.Name
                if not abilityDB[dbKey] or (now - abilityDB[dbKey]) > 3 then
                    local found = false
                    for _, v in ipairs(p.Character:GetDescendants()) do
                        if v.Name:lower():find(abilityName:lower()) then
                            found = true
                            break
                        end
                    end
                    if found then
                        abilityDB[dbKey] = now
                        ShowNotif(p.Name .. " used " .. abilityName .. "!", color)
                    end
                end
            end
        end
    end)
end

R.Heartbeat:Connect(function()
    DetectAbility("IW", "Infinity", Color3.fromRGB(255,200,0))
    DetectAbility("TD", "TimeHole", Color3.fromRGB(0,200,255))
    DetectAbility("DD", "DeathSlash", Color3.fromRGB(255,50,50))
    DetectAbility("AF", "Fury", Color3.fromRGB(255,120,0))
    DetectAbility("BF", "BubbleFury", Color3.fromRGB(180,100,255))
    DetectAbility("SR", "SpeedRush", Color3.fromRGB(0,255,180))
    
    if S.TN and Runtime and Runtime:FindFirstChild("Tornado") then
        local now = os.clock()
        if not abilityDB["TN_ACT"] or (now - abilityDB["TN_ACT"]) > 4 then
            abilityDB["TN_ACT"] = now
            Sys.__tornado_time = tick()
            ShowNotif("TORNADO spawned!", Color3.fromRGB(0,200,255))
        end
    end
    
    if S.AE2 then
        for _, p in ipairs(P:GetPlayers()) do
            if p ~= pl and p.Character then
                local now = os.clock()
                local dbKey = "AE2" .. p.Name
                if not abilityDB[dbKey] or (now - abilityDB[dbKey]) > 3 then
                    local hasAero = p.Character:FindFirstChild("AeroDynamicSlashVFX") or p.Character:FindFirstChild("AeroSlash")
                    if hasAero then
                        abilityDB[dbKey] = now
                        ShowNotif(p.Name .. " Aerodynamic!", Color3.fromRGB(100,220,255))
                        if S.AP and hasData then doFire() end
                    end
                end
            end
        end
    end
end)

local dR = false
local dS = 0
local dL = 0

R.PreSimulation:Connect(function()
    pcall(function()
        if not S.AD or dR then return end
        local ball = nil
        if Balls then
            for _, v in ipairs(Balls:GetChildren()) do
                if v:GetAttribute("realBall") then
                    ball = v
                    break
                end
            end
        end
        if not ball then return end
        local hr = pl.Character and pl.Character:FindFirstChild("HumanoidRootPart")
        if not hr then return end
        local vel = ball:FindFirstChild("zoomies") and ball.zoomies.VectorVelocity or ball.AssemblyLinearVelocity or Vector3.zero
        local spd = vel.Magnitude
        if spd < 3 then return end
        local now = tick()
        local targeted = ball:GetAttribute("target") == pl.Name
        local dist = (ball.Position - hr.Position).Magnitude
        local tti = dist / math.max(spd, 1)
        local dot = vel.Magnitude > 0 and vel.Unit:Dot((hr.Position - ball.Position).Unit) or 0
        local cooldown = spd >= 500 and 0.08 or 0.12
        if (now - dL) < cooldown then return end
        local shouldDodge = false
        
        if spd < 30 then
            if targeted and dot > 0.85 and tti <= 2.0 and dist < 25 then shouldDodge = true end
        elseif spd < 150 then
            if targeted and tti <= 3.0 then shouldDodge = true end
            if not targeted and dot > 0.1 and tti <= 2.2 and dist < 200 then shouldDodge = true end
        elseif spd < 500 then
            if targeted and tti <= 3.5 then shouldDodge = true end
            if not targeted and dot > 0.04 and tti <= 2.8 and dist < 400 then shouldDodge = true end
        else
            local dynamicTTI = math.clamp(1.2 + (spd - 500) / 800, 1.2, 3.2)
            if targeted and tti <= dynamicTTI then shouldDodge = true end
            if not targeted and dot > 0.05 and tti <= (dynamicTTI * 0.75) and dist < 500 then shouldDodge = true end
        end
        
        if not shouldDodge then return end
        
        dR = true
        dL = now
        local orig = hr.Position
        local cf = hr.CFrame
        local dodgeDist = spd >= 500 and 55 or 45
        local dirs = {
            cf.RightVector * dodgeDist,
            cf.RightVector * -dodgeDist,
            cf.LookVector * dodgeDist,
            cf.LookVector * -dodgeDist,
            Vector3.new(0, 50, 0),
            orig.Y > 8 and Vector3.new(0, -50, 0) or Vector3.new(0, 45, 0)
        }
        dS = (dS % #dirs) + 1
        pcall(function()
            hr.CFrame = CFrame.new(orig + dirs[dS])
            hr.AssemblyLinearVelocity = Vector3.zero
        end)
        local delay = spd >= 500 and 0.01 or 0.02
        task.delay(delay, function()
            if pl.Character then
                local hr2 = pl.Character:FindFirstChild("HumanoidRootPart")
                if hr2 then pcall(function() hr2.AssemblyLinearVelocity = Vector3.zero end) end
            end
            dR = false
        end)
    end)
end)

local siActive = false
local siPhysConn = nil
local siCamConn = nil
local siVPos = Vector3.new(0, 0, 0)
local siYaw = 0
local siPitch = -0.3

local function destroySI()
    siActive = false
    if siPhysConn then siPhysConn:Disconnect() siPhysConn = nil end
    if siCamConn then siCamConn:Disconnect() siCamConn = nil end
    if siLV and siLV.Parent then pcall(function() siLV:Destroy() end) siLV = nil end
    if siAtt and siAtt.Parent then pcall(function() siAtt:Destroy() end) siAtt = nil end
    local hum = pl.Character and pl.Character:FindFirstChildOfClass("Humanoid")
    if hum then pcall(function() hum.PlatformStand = false end) end
    pcall(function()
        local cam = workspace.CurrentCamera
        cam.CameraType = Enum.CameraType.Custom
        if hum then cam.CameraSubject = hum end
    end)
end

R.Heartbeat:Connect(function()
    if not S.SI then
        if siActive then destroySI() end
        return
    end
    if siActive then return end
    
    local char = pl.Character
    if not char then return end
    local hr = char:FindFirstChild("HumanoidRootPart")
    if not hr then return end
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hum then return end
    
    siActive = true
    siVPos = hr.Position
    local cam = workspace.CurrentCamera
    siYaw = math.atan2(-cam.CFrame.LookVector.X, -cam.CFrame.LookVector.Z)
    siPitch = math.asin(math.clamp(cam.CFrame.LookVector.Y, -1, 1)) * -1
    
    pcall(function()
        if not siAtt or not siAtt.Parent then
            siAtt = Instance.new("Attachment")
            siAtt.Position = Vector3.zero
            siAtt.Parent = hr
        end
        if not siLV or not siLV.Parent then
            siLV = Instance.new("LinearVelocity")
            siLV.Name = "ENX_SI_LV"
            siLV.Attachment0 = siAtt
            siLV.MaxForce = math.huge
            siLV.RelativeTo = Enum.ActuatorRelativeTo.World
            siLV.VectorVelocity = Vector3.zero
            siLV.Parent = hr
        end
    end)
    
    siCamConn = R.RenderStepped:Connect(function()
        pcall(function()
            if not siActive or not char or not char.Parent then return end
            local camDelta = UI:GetMouseDelta()
            siYaw = siYaw - camDelta.X * 0.0025
            siPitch = math.clamp(siPitch - camDelta.Y * 0.0025, -1.4, 0.4)
            local ws = savedWS or 16
            local fwd = Vector3.new(math.sin(siYaw), 0, math.cos(siYaw))
            local right = Vector3.new(math.cos(siYaw), 0, -math.sin(siYaw))
            local mv = Vector3.zero
            if not mb then
                if UI:IsKeyDown(Enum.KeyCode.W) or UI:IsKeyDown(Enum.KeyCode.Up) then mv = mv - fwd end
                if UI:IsKeyDown(Enum.KeyCode.S) or UI:IsKeyDown(Enum.KeyCode.Down) then mv = mv + fwd end
                if UI:IsKeyDown(Enum.KeyCode.A) or UI:IsKeyDown(Enum.KeyCode.Left) then mv = mv - right end
                if UI:IsKeyDown(Enum.KeyCode.D) or UI:IsKeyDown(Enum.KeyCode.Right) then mv = mv + right end
            else
                local hum2 = char:FindFirstChildOfClass("Humanoid")
                local md = hum2 and hum2.MoveDirection or Vector3.zero
                if md.Magnitude > 0.1 then
                    mv = Vector3.new(math.sin(siYaw) * -md.Z + math.cos(siYaw) * md.X, 0, math.cos(siYaw) * -md.Z - math.sin(siYaw) * md.X)
                end
            end
            if mv.Magnitude > 0 then mv = mv.Unit end
            siVPos = siVPos + mv * ws * (1/60)
            local camDist = 14
            local camPY = math.sin(siPitch) * camDist
            local camPH = math.cos(siPitch) * camDist
            local camPos = siVPos + Vector3.new(math.sin(siYaw) * camPH, camPY + 2, math.cos(siYaw) * camPH)
            local lookAt = siVPos + Vector3.new(0, 1.5, 0)
            local cam2 = workspace.CurrentCamera
            cam2.CameraType = Enum.CameraType.Scriptable
            cam2.CFrame = CFrame.new(camPos, lookAt)
        end)
    end)
    
    siPhysConn = R.PreSimulation:Connect(function()
        pcall(function()
            if not siActive then destroySI() return end
            local ball = nil
            if Balls then
                for _, v in ipairs(Balls:GetChildren()) do
                    if v:GetAttribute("realBall") then
                        ball = v
                        break
                    end
                end
            end
            if not ball then return end
            local hr2 = char:FindFirstChild("HumanoidRootPart")
            if not hr2 then return end
            local hum2 = char:FindFirstChildOfClass("Humanoid")
            if hum2 then hum2.PlatformStand = true end
            local ballPos = ball.Position
            local spd = (ball.AssemblyLinearVelocity or Vector3.zero).Magnitude
            siAngle = (siAngle + 0.04 + math.min(spd / 600, 0.05)) % (2 * math.pi)
            local tx = ballPos.X + math.cos(siAngle) * orbitR
            local tz = ballPos.Z + math.sin(siAngle) * orbitR
            local ty = ballPos.Y + orbitH
            local targetPos = Vector3.new(tx, ty, tz)
            local diff = targetPos - hr2.Position
            local vel2 = diff * (math.min(diff.Magnitude * 8, 150))
            if siLV and siLV.Parent then pcall(function() siLV.VectorVelocity = vel2 end) end
            if diff.Magnitude < 0.4 then
                pcall(function()
                    hr2.CFrame = CFrame.new(targetPos)
                    hr2.AssemblyLinearVelocity = Vector3.zero
                end)
            end
        end)
    end)
    
    pl.CharacterAdded:Connect(function() destroySI() end)
end)

R.Heartbeat:Connect(function()
    pcall(function()
        for p, hh in pairs(HL) do
            if not p or not p.Parent then
                pcall(function() hh:Destroy() end)
                HL[p] = nil
            end
        end
        if not S.PE and not S.SK then
            for p, hh in pairs(HL) do
                pcall(function() hh:Destroy() end)
                HL[p] = nil
            end
            return
        end
        for _, p in ipairs(P:GetPlayers()) do
            if p ~= pl and p.Character then
                local staff = isStaff(p)
                local show = S.PE or (S.SK and staff)
                if show then
                    if not HL[p] then
                        local hh = Instance.new("Highlight")
                        hh.Adornee = p.Character
                        hh.FillColor = staff and Color3.fromRGB(255,50,50) or C.A
                        hh.OutlineColor = staff and Color3.fromRGB(255,200,0) or C.A2
                        hh.FillTransparency = 0.5
                        hh.OutlineTransparency = 0
                        hh.Parent = workspace
                        HL[p] = hh
                    end
                    if S.DE then
                        local mh = pl.Character and pl.Character:FindFirstChild("HumanoidRootPart")
                        local th = p.Character:FindFirstChild("HumanoidRootPart")
                        if mh and th then
                            HL[p].Name = math.floor((th.Position - mh.Position).Magnitude) .. "st" .. (staff and "[S]" or "")
                        end
                    end
                else
                    if HL[p] then
                        pcall(function() HL[p]:Destroy() end)
                        HL[p] = nil
                    end
                end
            end
        end
    end)
end)

R.Heartbeat:Connect(function()
    pcall(function()
        local ball = nil
        if Balls then
            for _, v in ipairs(Balls:GetChildren()) do
                if v:GetAttribute("realBall") then
                    ball = v
                    break
                end
            end
        end
        if not S.BE then
            if BHL then BHL:Destroy() BHL = nil end
            return
        end
        if ball and not BHL then
            BHL = Instance.new("Highlight")
            BHL.Adornee = ball
            BHL.FillColor = C.A2
            BHL.OutlineColor = C.A
            BHL.FillTransparency = 0.3
            BHL.OutlineTransparency = 0
            BHL.Parent = workspace
        end
        if BHL and not ball then
            BHL:Destroy()
            BHL = nil
        end
    end)
end)

R.Heartbeat:Connect(function()
    pcall(function()
        local ball = nil
        if Balls then
            for _, v in ipairs(Balls:GetChildren()) do
                if v:GetAttribute("realBall") then
                    ball = v
                    break
                end
            end
        end
        if not S.BT or not ball then
            if TLb then TLb:Destroy() TLb = nil end
            return
        end
        if not TLb then
            TLb = Instance.new("SelectionBox")
            TLb.Color3 = C.A2
            TLb.LineThickness = 0.06
            TLb.SurfaceTransparency = 1
            TLb.Parent = workspace
        end
        TLb.Adornee = ball
    end)
end)

R.Heartbeat:Connect(function()
    pcall(function()
        if VTUI then VTUI.Visible = S.VT end
        if not S.VT then
            if VTL1 then VTL1.Text = "-- st/s" end
            if VTL2 then VTL2.Text = "X:0 Y:0 Z:0" end
            return
        end
        local ball = nil
        if Balls then
            for _, v in ipairs(Balls:GetChildren()) do
                if v:GetAttribute("realBall") then
                    ball = v
                    break
                end
            end
        end
        if not ball then
            if VTL1 then VTL1.Text = "-- st/s" end
            if VTL2 then VTL2.Text = "X:0 Y:0 Z:0" end
            if VTL3 then VTL3.Text = "Peak: " .. peakSpeed .. " st/s" end
            return
        end
        local zoomies = ball:FindFirstChild("zoomies")
        local vel = zoomies and zoomies.VectorVelocity or ball.AssemblyLinearVelocity or Vector3.zero
        local spd = math.floor(vel.Magnitude)
        if spd > peakSpeed then
            peakSpeed = spd
            if VTL3 then VTL3.Text = "Peak: " .. peakSpeed .. " st/s" end
        end
        local char = pl.Character
        local hr = char and char:FindFirstChild("HumanoidRootPart")
        local targetStr = ""
        if hr and vel.Magnitude > 1 then
            local towardDot = vel.Unit:Dot((hr.Position - ball.Position).Unit)
            if towardDot > 0.3 then
                targetStr = " → YOU"
            elseif towardDot < -0.3 then
                targetStr = " ← AWAY"
            end
        end
        local target = ball:GetAttribute("target") or ""
        local targetName = target ~= "" and (" [" .. target .. "]") or ""
        if VTL1 then
            VTL1.Text = spd .. " st/s" .. targetStr
            VTL1.TextColor3 = spd > 150 and Color3.fromRGB(255,50,50) or (spd > 60 and Color3.fromRGB(255,180,0) or C.A)
        end
        if VTL2 then
            VTL2.Text = string.format("X:%.0f Y:%.0f Z:%.0f", vel.X, vel.Y, vel.Z)
        end
    end)
end)

R.Heartbeat:Connect(function()
    pcall(function()
        if not S.RE then return end
        local hue = (tick() % 4) / 4
        for _, hh in pairs(HL) do
            hh.FillColor = Color3.fromHSV(hue, 1, 1)
            hh.OutlineColor = Color3.fromHSV((hue + 0.5) % 1, 1, 1)
        end
        if BHL then
            BHL.FillColor = Color3.fromHSV((hue + 0.25) % 1, 1, 1)
        end
    end)
end)

local AE_CONFIG = {
    gui_name = "AbilityESPGui",
    gui_size = UDim2.new(0, 200, 0, 40),
    studs_offset = Vector3.new(0, 3.2, 0),
    text_color = Color3.fromRGB(255,255,255),
    stroke_color = Color3.fromRGB(0,0,0),
    font = Enum.Font.GothamBold,
    text_size = 14,
    update_rate = 1/30
}

local function ae_create_billboard(player)
    local character = player.Character
    if not character or not character.Parent then return nil end
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if not humanoid then return nil end
    local head = character:FindFirstChild("Head")
    if not head then return nil end
    local existing = head:FindFirstChild(AE_CONFIG.gui_name)
    if existing then existing:Destroy() end
    local billboard = Instance.new("BillboardGui")
    billboard.Name = AE_CONFIG.gui_name
    billboard.Adornee = head
    billboard.Size = AE_CONFIG.gui_size
    billboard.StudsOffset = AE_CONFIG.studs_offset
    billboard.AlwaysOnTop = true
    billboard.Parent = head
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.TextColor3 = AE_CONFIG.text_color
    label.TextStrokeColor3 = AE_CONFIG.stroke_color
    label.TextStrokeTransparency = 0.5
    label.Font = AE_CONFIG.font
    label.TextSize = AE_CONFIG.text_size
    label.TextWrapped = true
    label.TextXAlignment = Enum.TextXAlignment.Center
    label.TextYAlignment = Enum.TextYAlignment.Center
    label.Parent = billboard
    humanoid.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.None
    return label, billboard
end

local function ae_update_label(player, label)
    if not player or not player.Parent or not label or not label.Parent then return false end
    local character = player.Character
    if not character or not character.Parent or not character:FindFirstChildOfClass("Humanoid") then return false end
    if S.AE then
        label.Visible = true
        local ability_name = player:GetAttribute("EquippedAbility")
        label.Text = ability_name and (player.DisplayName .. "  [" .. ability_name .. "]") or player.DisplayName
    else
        label.Visible = false
    end
    return true
end

local function ae_setup_character(player)
    if not S.AE then return end
    task.wait(0.1)
    local character = player.Character
    if not character or not character.Parent or not character:FindFirstChildOfClass("Humanoid") then return end
    local label, billboard = ae_create_billboard(player)
    if not label then return end
    if not AE_STATE.players[player] then AE_STATE.players[player] = {} end
    AE_STATE.players[player].label = label
    AE_STATE.players[player].billboard = billboard
    AE_STATE.players[player].character = character
    character.AncestryChanged:Connect(function()
        if not character.Parent then
            if AE_STATE.players[player] then
                if AE_STATE.players[player].billboard then AE_STATE.players[player].billboard:Destroy() end
                AE_STATE.players[player].label = nil
                AE_STATE.players[player].billboard = nil
                AE_STATE.players[player].character = nil
            end
        end
    end)
    pcall(function()
        character:GetAttributeChangedSignal("EquippedAbility"):Connect(function()
            if AE_STATE.players[player] and AE_STATE.players[player].label then
                ae_update_label(player, AE_STATE.players[player].label)
            end
        end)
    end)
end

local function ae_remove_player(player)
    if AE_STATE.players[player] then
        if AE_STATE.players[player].billboard then
            pcall(function() AE_STATE.players[player].billboard:Destroy() end)
        end
        AE_STATE.players[player] = nil
    end
    AEGUI[player] = nil
end

local function ae_add_player(player)
    if player == pl then return end
    ae_remove_player(player)
    AEGUI[player] = true
    player.CharacterAdded:Connect(function() ae_setup_character(player) end)
    if player.Character then task.spawn(function() ae_setup_character(player) end) end
end

local function ae_update_loop()
    while S.AE do
        task.wait(AE_CONFIG.update_rate)
        local to_remove = {}
        for player, player_data in pairs(AE_STATE.players) do
            if not player or not player.Parent then
                table.insert(to_remove, player)
            else
                local character = player.Character
                if not character or not character.Parent or not character:FindFirstChildOfClass("Humanoid") then
                    if player_data.billboard then
                        pcall(function() player_data.billboard:Destroy() end)
                        player_data.billboard = nil
                        player_data.label = nil
                    end
                else
                    if player_data.label then
                        ae_update_label(player, player_data.label)
                    end
                end
            end
        end
        for _, p in ipairs(to_remove) do ae_remove_player(p) end
    end
end

local function ae_enable()
    AE_STATE.active = true
    for _, p in ipairs(P:GetPlayers()) do ae_add_player(p) end
    P.PlayerAdded:Connect(function(p) if p ~= pl then ae_add_player(p) end end)
    task.spawn(ae_update_loop)
end

local function ae_disable()
    AE_STATE.active = false
    for player in pairs(AE_STATE.players) do ae_remove_player(player) end
    AEGUI = {}
end

R.Heartbeat:Connect(function()
    if S.AE and not AE_STATE.active then
        AE_STATE.active = true
        ae_enable()
    elseif not S.AE and AE_STATE.active then
        AE_STATE.active = false
        ae_disable()
    end
end)

for _, p in ipairs(P:GetPlayers()) do
    if p ~= pl then
        p.CharacterAdded:Connect(function()
            task.wait(0.5)
            if S.AE then ae_setup_character(p) end
        end)
    end
end

P.PlayerAdded:Connect(function(p)
    if p ~= pl then
        p.CharacterAdded:Connect(function()
            task.wait(0.5)
            if S.AE then ae_setup_character(p) end
        end)
    end
end)

pl.CharacterAdded:Connect(function()
    task.wait(0.5)
    if S.AE then
        for _, p in ipairs(P:GetPlayers()) do
            if p ~= pl then ae_setup_character(p) end
        end
    end
end)

local function KSS_stop()
    if KSS_curSound then
        pcall(function() KSS_curSound:Stop() KSS_curSound:Destroy() end)
        KSS_curSound = nil
    end
end

local function KSS_play(pos)
    KSS_stop()
    local part = Instance.new("Part")
    part.Anchored = true
    part.CanCollide = false
    part.Transparency = 1
    part.Size = Vector3.new(0.1, 0.1, 0.1)
    part.Position = pos or Vector3.new(0, 0, 0)
    part.Parent = workspace
    local snd = Instance.new("Sound")
    snd.SoundId = "rbxassetid://" .. KSS_selected
    snd.Volume = 0.7
    snd.RollOffMode = Enum.RollOffMode.Linear
    snd.MaxDistance = 500
    snd.Parent = part
    KSS_curSound = snd
    snd:Play()
    DB:AddItem(part, 5)
end

local function KSS_onKill(character)
    if not S.KS then return end
    local now = tick()
    if (now - KSS_lastKill) < 0.3 then return end
    KSS_lastKill = now
    local pos = character and character.PrimaryPart and character.PrimaryPart.Position or workspace.CurrentCamera.CFrame.Position
    KSS_play(pos)
end

local function KSS_hookPlayer(p)
    local function onChar(char)
        local hum = char:WaitForChild("Humanoid", 5)
        if hum then
            hum.Died:Connect(function()
                if p ~= pl then KSS_onKill(char) end
            end)
        end
    end
    if p.Character then task.spawn(function() onChar(p.Character) end) end
    p.CharacterAdded:Connect(onChar)
end

local function KSS_enable()
    for _, p in ipairs(P:GetPlayers()) do KSS_hookPlayer(p) end
    KSS_conns.playerAdded = P.PlayerAdded:Connect(function(p) KSS_hookPlayer(p) end)
end

local function KSS_disable()
    KSS_stop()
    for _, c in pairs(KSS_conns) do pcall(function() c:Disconnect() end) end
    KSS_conns = {}
end

R.Heartbeat:Connect(function()
    if S.KS and not KSS_conns.playerAdded then
        KSS_enable()
    elseif not S.KS and KSS_conns.playerAdded then
        KSS_disable()
    end
end)

R.Heartbeat:Connect(function()
    if not S.NR then
        if noRenderConn then pcall(function() noRenderConn:Disconnect() end) noRenderConn = nil end
    else
        if not noRenderConn and Runtime then
            noRenderConn = Runtime.ChildAdded:Connect(function(v) DB:AddItem(v, 0) end)
        end
    end
    if not S.NR2 then
        if noRender2Conn then pcall(function() noRender2Conn:Disconnect() end) noRender2Conn = nil end
        pcall(function()
            local efx = pl.PlayerScripts:FindFirstChild("EffectScripts")
            if efx then
                local cfx = efx:FindFirstChild("ClientFX")
                if cfx then cfx.Disabled = false end
            end
        end)
    else
        pcall(function()
            local efx = pl.PlayerScripts:FindFirstChild("EffectScripts")
            if efx then
                local cfx = efx:FindFirstChild("ClientFX")
                if cfx then cfx.Disabled = true end
            end
        end)
    end
end)

local sc_swordInstances = nil
local sc_swordsController = nil

task.spawn(function()
    pcall(function()
        local si = RS:WaitForChild("Shared", 10):WaitForChild("ReplicatedInstances", 10):WaitForChild("Swords", 10)
        sc_swordInstances = require(si)
    end)
end)

task.spawn(function()
    local t0 = tick()
    while not sc_swordsController and (tick() - t0) < 15 do
        task.wait()
        pcall(function()
            for _, v in ipairs(getconnections(RS.Remotes.FireSwordInfo.OnClientEvent)) do
                if v.Function and islclosure(v.Function) then
                    local upv = getupvalues(v.Function)
                    if #upv == 1 and type(upv[1]) == "table" then
                        sc_swordsController = upv[1]
                        break
                    end
                end
            end
        end)
    end
end)

local function sc_getSlashName(fxName)
    if not fxName or fxName == "" then return "SlashEffect" end
    if not sc_swordInstances then return "SlashEffect" end
    local ok, slashName = pcall(function()
        local data = sc_swordInstances:GetSword(fxName)
        return (data and data.SlashName) or "SlashEffect"
    end)
    return ok and slashName or "SlashEffect"
end

local sc_slashName = "SlashEffect"
local sc_riverPlayParryFunc = nil

task.spawn(function()
    local t0 = tick()
    repeat
        task.wait()
        for _, v in ipairs(getconnections(RS.Remotes.ParrySuccessAll.OnClientEvent)) do
            if v.Function then
                local ok, info = pcall(function() return getinfo(v.Function) end)
                if ok and info and info.name == "parrySuccessAll" then
                    sc_riverPlayParryFunc = v.Function
                    v:Disable()
                    break
                end
            end
        end
    until sc_riverPlayParryFunc or (tick() - t0) > 5
    if sc_riverPlayParryFunc then
        RS.Remotes.ParrySuccessAll.OnClientEvent:Connect(function(...)
            setthreadidentity(2)
            local args = {...}
            if tostring(args[4]) ~= tostring(pl) then
                return sc_riverPlayParryFunc(unpack(args))
            elseif skinChangerEnabled and S.SC and swordFX ~= "" then
                args[1] = sc_slashName
                args[3] = swordFX
            end
            return sc_riverPlayParryFunc(unpack(args))
        end)
    end
end)

local function sc_setSword()
    if not skinChangerEnabled or not S.SC then return end
    if not setupvalue then return end
    if not sc_swordInstances then return end
    pcall(function()
        setupvalue(rawget(sc_swordInstances, "EquipSwordTo"), 3, false)
        if swordModel ~= "" then
            sc_swordInstances:EquipSwordTo(pl.Character, swordModel)
        end
        if swordAnimations ~= "" and sc_swordsController and sc_swordsController.SetSword then
            sc_swordsController:SetSword(swordAnimations)
        end
    end)
end

local function sc_updateFX()
    if swordFX ~= "" then
        sc_slashName = sc_getSlashName(swordFX)
    end
end

task.spawn(function()
    while task.wait(1) do
        if skinChangerEnabled and S.SC then
            pcall(function()
                local char = pl.Character
                if not char then return end
                if swordModel ~= "" then
                    if pl:GetAttribute("CurrentlyEquippedSword") ~= swordModel then sc_setSword() end
                    if not char:FindFirstChild(swordModel) then sc_setSword() end
                    for _, v in ipairs(char:GetChildren()) do
                        if v:IsA("Model") and v.Name ~= swordModel then
                            pcall(function() v:Destroy() end)
                        end
                        task.wait()
                    end
                end
            end)
        end
    end
end)

local scApplyOld = nil

R.Heartbeat:Connect(function()
    if S.SC and skinChangerEnabled and swordFX ~= "" then sc_updateFX() end
end)

task.spawn(function()
    local was = false
    while task.wait(0.5) do
        if S.FP and not was then
            was = true
            pcall(function()
                L.GlobalShadows = false
                L.Brightness = 1
                L.FogEnd = 100000
                for _, v in ipairs(game:GetDescendants()) do
                    pcall(function()
                        if v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Smoke") or v:IsA("Fire") then
                            v:Destroy()
                        elseif v:IsA("BasePart") then
                            v.Material = Enum.Material.Plastic
                            v.Reflectance = 0
                            v.Color = Color3.fromRGB(0,0,0)
                        end
                    end)
                end
            end)
        elseif not S.FP then
            was = false
        end
    end
end)

R.Heartbeat:Connect(function()
    if not S.FB then return end
    pcall(function()
        L.Ambient = Color3.new(1,1,1)
        L.OutdoorAmbient = Color3.new(1,1,1)
        L.Brightness = 2
    end)
end)

local lf = 0

task.spawn(function()
    while task.wait(0.5) do
        pcall(function()
            local ball = nil
            if Balls then
                for _, v in ipairs(Balls:GetChildren()) do
                    if v:GetAttribute("realBall") then
                        ball = v
                        break
                    end
                end
            end
            local bs = ball and math.floor((ball.AssemblyLinearVelocity or Vector3.zero).Magnitude) or 0
            local cs = math.floor((serverFireCounter - lf) / 0.5)
            lf = serverFireCounter
            if ST then
                if not hasData then
                    ST.Text = "EclipseNexus Capturing..."
                else
                    ST.Text = string.format("CPS:%d Ball:%dst/s", cs, bs)
                end
            end
            if SD then
                SD.BackgroundColor3 = hasData and C.A or Color3.fromRGB(60,60,90)
            end
        end)
    end
end)

musicSound = Instance.new("Sound")
musicSound.Name = "ENX_Music"
musicSound.Volume = 0.8
musicSound.Looped = true
musicSound.Parent = workspace

local musicList = {
    {name = "Multo", id = 7492665376},
    {name = "Love Me Not", id = 1843619980},
    {name = "Back To Being Friends", id = 6838623385},
    {name = "Sana (Single)", id = 3108025828}
}

local function stopMusic()
    pcall(function()
        musicSound:Stop()
        musicSound.SoundId = ""
    end)
    currentMusicIdx = 0
end

local function playMusic(numId)
    task.spawn(function()
        pcall(function()
            musicSound:Stop()
            task.wait(0.05)
            musicSound.SoundId = "rbxassetid://" .. tostring(numId)
            local t = tick()
            repeat task.wait(0.1) until musicSound.IsLoaded or (tick() - t) > 8
            if musicSound.IsLoaded then musicSound:Play() end
        end)
    end)
end

MkSec(MU, "Now Playing")
nowPlayingLabel = N("TextLabel", MU, {Size = UDim2.new(1, 0, 0, 30), BackgroundColor3 = C.C, BorderSizePixel = 0, Font = Enum.Font.GothamBold, TextSize = 11, TextColor3 = C.A, Text = "♪ None", TextXAlignment = Enum.TextXAlignment.Center, ZIndex = 2})
N("UICorner", nowPlayingLabel, {CornerRadius = UDim.new(0, 10)})

MkSec(MU, "Songs")
for i, song in ipairs(musicList) do
    local sFrame = N("Frame", MU, {Size = UDim2.new(1, 0, 0, 38), BackgroundColor3 = C.C, BorderSizePixel = 0, ZIndex = 2})
    N("UICorner", sFrame, {CornerRadius = UDim.new(0, 10)})
    N("UIStroke", sFrame, {Thickness = 1, Color = C.OF})
    N("TextLabel", sFrame, {Size = UDim2.new(1, -50, 1, 0), Position = UDim2.new(0, 12, 0, 0), BackgroundTransparency = 1, Text = song.name, Font = Enum.Font.GothamMedium, TextSize = 11, TextColor3 = C.TX, TextXAlignment = Enum.TextXAlignment.Left, ZIndex = 3})
    local playBtn = N("TextButton", sFrame, {Size = UDim2.fromOffset(36, 26), Position = UDim2.new(1, -42, 0.5, -13), BackgroundColor3 = C.A, Text = "▶", Font = Enum.Font.GothamBold, TextSize = 13, TextColor3 = Color3.new(1,1,1), AutoButtonColor = false, ZIndex = 4})
    N("UICorner", playBtn, {CornerRadius = UDim.new(0, 6)})
    songBtns[i] = playBtn
    local idx = i
    local sData = song
    playBtn.MouseButton1Click:Connect(function()
        if currentMusicIdx == idx then
            stopMusic()
            playBtn.Text = "▶"
            playBtn.BackgroundColor3 = C.A
            if nowPlayingLabel then nowPlayingLabel.Text = "♪ None" end
            return
        end
        stopMusic()
        for j, b in pairs(songBtns) do
            b.Text = "▶"
            b.BackgroundColor3 = C.A
        end
        currentMusicIdx = idx
        playBtn.Text = "■"
        playBtn.BackgroundColor3 = Color3.fromRGB(200,40,40)
        if nowPlayingLabel then nowPlayingLabel.Text = "♪ " .. sData.name end
        playMusic(sData.id)
    end)
end

MkSec(MU, "Custom ID")
local cFrame = N("Frame", MU, {Size = UDim2.new(1, 0, 0, 38), BackgroundColor3 = C.C, BorderSizePixel = 0, ZIndex = 2})
N("UICorner", cFrame, {CornerRadius = UDim.new(0, 10)})
N("UIStroke", cFrame, {Thickness = 1, Color = C.OF})
local cBox = N("TextBox", cFrame, {Size = UDim2.new(1, -72, 1, -8), Position = UDim2.new(0, 8, 0, 4), BackgroundColor3 = C.BG, BorderSizePixel = 0, PlaceholderText = "Paste audio ID...", Font = Enum.Font.Gotham, TextSize = 10, TextColor3 = C.TX, PlaceholderColor3 = C.SB, ClearTextOnFocus = false, ZIndex = 3})
N("UICorner", cBox, {CornerRadius = UDim.new(0, 6)})
local cBtn = N("TextButton", cFrame, {Size = UDim2.fromOffset(58, 26), Position = UDim2.new(1, -62, 0.5, -13), BackgroundColor3 = C.A, Text = "▶ Play", Font = Enum.Font.GothamBold, TextSize = 9, TextColor3 = Color3.new(1,1,1), AutoButtonColor = false, ZIndex = 4})
N("UICorner", cBtn, {CornerRadius = UDim.new(0, 6)})
cBtn.MouseButton1Click:Connect(function()
    local id = (cBox.Text or ""):match("%d+")
    if id and #id > 0 then
        stopMusic()
        for _, b in pairs(songBtns) do
            b.Text = "▶"
            b.BackgroundColor3 = C.A
        end
        currentMusicIdx = -1
        if nowPlayingLabel then nowPlayingLabel.Text = "♪ ID: " .. id end
        playMusic(id)
    end
end)

MkSlider(MU, "Music Volume", 0, 100, 80, function(v)
    if musicSound then musicSound.Volume = v / 100 end
end)

local stopBtn = N("TextButton", MU, {Size = UDim2.new(1, 0, 0, 32), BackgroundColor3 = Color3.fromRGB(160,30,30), Text = "⏹ Stop", Font = Enum.Font.GothamBold, TextSize = 12, TextColor3 = Color3.new(1,1,1), AutoButtonColor = false, BorderSizePixel = 0, ZIndex = 2})
N("UICorner", stopBtn, {CornerRadius = UDim.new(0, 10)})
stopBtn.MouseButton1Click:Connect(function()
    stopMusic()
    if nowPlayingLabel then nowPlayingLabel.Text = "♪ None" end
    for _, b in pairs(songBtns) do
        b.Text = "▶"
        b.BackgroundColor3 = C.A
    end
end)

pcall(function()
    setfflag("TaskSchedulerTargetFps", "5099990")
end)

pcall(function()
    RS.Remotes.ParrySuccessAll.OnClientEvent:Connect(function(...)
        local args = {...}
        if tostring(args[4]) ~= tostring(pl) and S.KS then
            local char = args[4] and args[4].Parent
            if char then
                task.spawn(function()
                    local pos = char.PrimaryPart and char.PrimaryPart.Position or workspace.CurrentCamera.CFrame.Position
                    local now = tick()
                    if (now - KSS_lastKill) >= 0.3 then
                        KSS_lastKill = now
                        local part = Instance.new("Part")
                        part.Anchored = true
                        part.CanCollide = false
                        part.Transparency = 1
                        part.Size = Vector3.new(0.1, 0.1, 0.1)
                        part.Position = pos
                        part.Parent = workspace
                        local snd = Instance.new("Sound")
                        snd.SoundId = "rbxassetid://" .. KSS_selected
                        snd.Volume = 0.7
                        snd.Parent = part
                        snd:Play()
                        DB:AddItem(part, 5)
                    end
                end)
            end
        end
    end)
end)

task.spawn(function()
    while task.wait(0.8) do
        if S.SC and skinChangerEnabled and swordAnimations ~= "" then
            pcall(function()
                local sc = nil
                if RS:FindFirstChild("Controllers") then
                    for _, c in ipairs(RS.Controllers:GetChildren()) do
                        if c.Name:match("^SwordsController") then
                            sc = c
                            break
                        end
                    end
                end
                if sc and sc.SetSword then
                    pcall(function() sc:SetSword(swordAnimations) end)
                end
            end)
        end
    end
end)

R.Heartbeat:Connect(function()
    pcall(function()
        if not S.RE then return end
        local hue = (tick() % 4) / 4
        for _, hh in pairs(HL) do
            hh.FillColor = Color3.fromHSV(hue, 1, 1)
            hh.OutlineColor = Color3.fromHSV((hue + 0.5) % 1, 1, 1)
        end
        if BHL then
            BHL.FillColor = Color3.fromHSV((hue + 0.25) % 1, 1, 1)
        end
    end)
end)

pcall(function()
    for _, p in ipairs(P:GetPlayers()) do
        if p ~= pl then
            p.CharacterAdded:Connect(function() pAbCache[p] = nil end)
        end
    end
end)

P.PlayerAdded:Connect(function(p)
    if p ~= pl then
        p.CharacterAdded:Connect(function() pAbCache[p] = nil end)
    end
end)

print("[EclipseNexus v2.0] Loaded - 24 parts OK")

pcall(function()
    task.wait(2)
    if hasData then
        if ST then ST.Text = "Ready! Remote captured" end
    elseif ST then
        ST.Text = "Waiting for first parry..."
    end
end)
